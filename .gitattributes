# Git LFS is intentionally disabled for the active repository history.
# Dense generated outputs are ignored; compact evidence files use regular Git.
*.csv !text !filter !merge !diff
*.jsonl !text !filter !merge !diff
*.npz !text !filter !merge !diff
*.pt !text !filter !merge !diff
