# 阶段 5：固定配置复核与正式确认接续

2026-09-23。用户明确决定结束追加优化、进入阶段 5，并授权开始推进。本协议取代当前入口中“阶段 5 暂缓”的执行安排；阶段 4.5、4.5-B、4.5-C 未通过原采用门的历史结论全部保留。阶段完成依据是实验完整、公平和可复现，不要求 Chronaris 连续融合模型胜出。

## 本轮固定配置与交付

**本轮执行阶段 5 的第一批：固定参考的跨划分、跨种子复核。** 采用阶段 4 原参考的原始损失选模、32 维隐状态与双路线，不采用失败的结构、学习率、分支归一化或公共辅助项调整。自监督训练后冻结表示；任务引导从本单元自监督检查点初始化，微调后移除任务头、冻结表示并重新拟合同类下游模型。

公开主配置保持 `stage4_reference`：Chronaris 使用既有安全时滞融合，公共自监督与任务目标不变，联合微调保留 0.2 公共项；没有加入额外机制组合。因此该公开结果检验既有连续融合表示，不宣称检验了未开启的物理残差或额外事件机制。仿真物理与时间机制须按单独冻结的对应配置评价，不能用公开配置替代。

下表规定首批复核的范围，支撑判断原参考的收益和不足是否跨开发划分与种子稳定；强参照在运行前指定，不依据本轮结果更换。

| 数据 | 方法 | 划分与种子 | 单元数 |
| --- | --- | --- | ---: |
| CLARE 生理负荷数据 | Chronaris、ContiFormer 连续时间变换器 | 原固定三折；17/29/43 | 18 |
| CogPilot 虚拟飞行数据 | Chronaris、第二输入流单流 | 原固定三折；17/29/43 | 18 |
| 合计 | 每单元分别导出自监督和任务引导表示 | 36 个模型单元、72 条表示路线 | 36 |

首折种子 17 的四个完整结果从阶段 4 验收继承，新增 32 个训练单元。继承前核验阶段 4 的 1075 个来源文件，以及四个单元的观测、任务、角色、消费者规则、模型和结果散列；保留原始源码归属与历史成本，不把旧单元记为新训练。新训练均从合法随机初始化开始，不在旧检查点上续训。继承是既有开发结果的来源保留，不是当前源码逐位重算的声明。

训练使用现有配方：有效批量 16、实际批量 4，预训练至少 300 次且覆盖完整训练角色，任务头预热 50 次、联合更新至少 200 次且覆盖完整训练角色；实际更新数分别取起始预算与训练窗口数除以 16 向上取整的最大值。每 50 次验证，执行完整预算，按原损失选择检查点。预训练学习率 0.001、微调学习率 0.0001，其他参数由 `training_recipe` 原样绑定到机器计划。

公共下游沿用逻辑回归、逐字段岭回归及 MiniROCKET 时序卷积特征补充；各表示分别拟合权重。公开开发验证仍参与下游参数选择，所以本批只形成开发复核结果。逐折逐种子保留宏平均 F1 与均方根误差，先受试者再汇总；九个设置的均值、标准差和范围只作描述，不把重叠受试者、折和种子当作独立样本。

## 执行、验收与预算

复用 `run_v4_pipeline.py --until stage5`，按完整显卡回归、来源及矩阵冻结、两域普通与图执行对照及独立恢复、36 个固定单元、最终汇总的顺序运行。Chronaris 仅在当前源码对应的两域执行资格通过后使用显卡运算图；两种强参照复用普通执行。沿用既定数值容限，不将既有提速倍数当作新完整单元的耗时。

本批新增 32 个单元，在两个数据域、两种方法各有八个；最多完成固定矩阵，不自动追加候选、调参或重试未知错误。保持 30 秒心跳、更新边界保存、失败现场和显式恢复。模型与任务结果不利时继续归档；来源不一致、非有限或执行资格失败时停止。不得改历史文件或放宽数值检查。

**初始预算保留不确定性，完成首个新单元后用同域同方法计时更新。** 阶段 4 的旧执行估算：四类单元各新增八次合计约 154 小时，加 30% 预留约 200 小时（8.3 天）；这不是当前图执行工期，CLARE 原计时包含未加速微调。新入口逐单元记录预训练、微调、导出、消费者与墙钟，不跨数据域移植加速比例。固定计算量而非任意小时截断，不重新开启无限开发。

本轮先完成三项交付：固定机器计划、真实新折新种子与恢复验证、后台复核队列及结果汇总。完整回归必须来自实际启动的冻结源码副本。大型数据与模型留在现有忽略目录，仓库仅提交代码、协议和紧凑证据。

## 正式实验与后续接续

**正式主表保留所有已接入方法，不以首批强参照复核替代完整比较。** 原六方法为 Chronaris、生理单流、第二输入流单流、朴素时间同步、MulT 多模态变换器、ContiFormer；另外保留 TimeCMA 语言模型增强方法、Chronos-2 时序预训练模型，以及仅在 CLARE 使用的 SensorLLM 传感器语言对齐 DeepSeek 骨干变体。外部预训练和目标域标签使用分层展示。

按公开五折三种子、鼎新单记录三种子计算，完整原生方法范围为 279 个模型单元，477 条实际表示路线；朴素同步只有一条自监督路线，Chronos-2 保留冻结与历史适配两条路线，不复制等价路线凑数量。此数量不含仿真、消融或压力。该清单固定方法覆盖，正式训练预算、消费者训练内选择与外层接口仍须在打开正式成绩之前完成机器冻结和验收；不能直接运行旧六方法确认入口并宣称覆盖近期模型。

后续依次完成：完整方法正式接口接续与预算冻结；主模型及核心消融模型文件冻结；公开受试者分组确认、鼎新单记录描述性留出、仿真机制与压力；统一主表、消融表、分组统计、成本及案例。因果隔离、缺失掩码、来源与恢复要求持续保留；不得用“进入阶段 5”代替这些正确性验收。

本批完成后自动停在开发复核报告，不自动打开独立确认数据。阶段 5 已获推进授权，后续在上述条件满足后继续实施，不以本批模型排名作为新的通过门。阶段 6 的稳定论文章节、文献和鼎新案例可同步推进，不依赖新增一手数据或专家标签。
