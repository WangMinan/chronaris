# 冻结仿真实跑与安全门复核

复核日期：2026-09-04。最新判断：v3.2.3 修复与全部剩余受控仿真已经完成，显式时移、事件配对、连续演化和物理一致性四项门通过；未来信息隔离和安全旁路两项失败。未来信息问题已只读定位到事件分数的全窗归一化，未实施新模型修复。公开数据和鼎新外层评价继续关闭，完整论文任务尚未完成。

后续确认：用户已明确批准 [v3.2.3](../../../requirements/thesis-frozen-paper-evaluation-v3.2.3.md)，仅修复无观测表示和严格安全门；不调模型、不放宽阈值、不开放外层。下文保留确认前的发现过程和全部原始结论，后续执行以新协议为准。

阅读顺序：以下“复核范围与方法”至“下一步与时间边界”为确认前快照；其后的恢复记录和文末“最终硬门与只读定位”是当前结论。中间的应用与机制数值均保留，但受新发现的语义未来依赖限制，不写成严格因果连续融合已经成立。

## 复核范围与方法

本次复核先检查训练进程、编排状态和训练检查点，再从原始观测掩码定位导出异常，最后独立重算干净场景与结构消融的三随机种子指标。仅检查既有冻结配置，不选择新模型、不调整权重、不读取公开数据或鼎新外层结果。

- 源码提交：`7937334239e2cdc0263754de03328769ab90987c`。
- 编排状态：`docs/artifacts/runs/2026-09-03_thesis-simulation-v3p2p1/run_state.json`。
- 原训练进程 `22688` 已退出；日志末尾为 `RepresentationContractError`，不是仍在训练或等待。
- 十二个结构消融 `last.pt` 均为 `epoch=50`、`best_epoch=50`、`training_status=completed`；检查点未重写。

## 已完成的证据

四项结构消融与三个随机种子的十二个组合全部完成训练，训练验收 6/6；三角色表示共 36 份，验收 5/5；消费者产生 768 条指标和 72 条配对统计，验收 6/6。干净场景消费者保留 1152 条指标，验收 7/7。上述验收证明产物完整性，不等价于机制门或应用优势成立。

指标来源与文件哈希如下，原始数值不覆盖：

- 干净场景：`docs/artifacts/runs/2026-09-03_thesis-simulation-consumers-v3p2p1/metric_long.csv`，SHA-256 为 `55e1437074433399055b302180898f4fad2432ccb96c80f59f3c712ee7b5e77b`。
- 结构消融：`docs/artifacts/runs/2026-09-03_thesis-simulation-ablation-consumers-v3p2p1/metric_long.csv`，SHA-256 为 `7199890f66469de11218d069ca6ca5c94227fed01863a93c8365062514ee29bf`。

## 无观测窗口导出异常

异常由设计内的连续缺失触发，不是已经证明的数据损坏。压力表示已完成首个随机种子的前 25 个场景，在第 26 个场景 `contiguous_gap_30s` 的生理单流导出时退出。对 192 个固定窗口进行原始观测因果查询复核，生理全窗无观测为 1 个（0.52%），航电全窗无观测为 0 个，双流同时无观测为 0 个。

受影响样本为 `g2_event_spline__locked_test_profile_007__trajectory_046__seed_300046__contiguous_gap_30s::context_030.000`。额外检查低信噪比场景 `observation_snr_05db` 和混合严重扰动场景 `mixed_severe`，各 192 个样本均未出现全窗无观测。未据此声称其他全部场景已经复核。

根因位于共享表示合同：`TrainedFusionAdapter` 保留编码器的真实有效查询掩码，且空掩码池化采用分母下限 1；`FusionStreamBatch.__post_init__` 却拒绝任何零有效点样本。把某个点改成有效会伪造观测支持，删除该样本会改变冻结比较总体，均不采用。

待确认的最小修订：允许无观测样本保持全假掩码、零序列和零池化表示；保留原样本与 `[B,96,64]` 形状，继续使用已冻结消费者，不重拟合；输出无观测覆盖率及其单独诊断。必须测试非空样本数值不变、空样本严格为零、导出恢复一致和样本不丢失。该修订涉及输出可用性语义，尚未实施，也未创建新的协议版本。

观测侧复核可通过以下既有入口重现，不读取任务目标：

```python
from chronaris.evaluation.application_tasks.simulation_stress_context_data import load_simulation_stress_context_data
from chronaris.modeling.fusion_encoders.causal_query import causal_query_stream

data = load_simulation_stress_context_data(
    "artifacts/application_evaluation/2026-07-12_aviation-simulation-locked-stress",
    scenario_id="contiguous_gap_30s",
)
for stream in ("physiology", "vehicle"):
    mask = causal_query_stream(data.batch, stream_name=stream).modality_mask
    print(stream, int((~mask.any(dim=1)).sum()), len(data.batch.sample_ids))
```

## 三随机种子的应用与消融分析

现有结果支持“旁路在部分消费者上保护信息，应用增量依赖消费者”，不支持所有配置全面领先。下表逐个随机种子与该随机种子的最佳单流配对，再计算中位增量；分类使用宏平均 F1（macro-F1）差，回归使用均方根误差（RMSE）的“单流误差减 Chronaris 误差”，因此正值均表示 Chronaris 更好。随机种子固定为 17、29、43，只包含受控仿真留出集。

| 消费者 | 任务与指标 | 相对最佳单流的配对增量中位数 | 正增量种子数 |
| --- | --- | ---: | ---: |
| 固定线性消费者 | 未来负荷分类，宏平均 F1 | −0.02126 | 1/3 |
| 固定线性消费者 | 未来负荷回归，均方根误差 | +0.01903 | 3/3 |
| MiniROCKET 时序卷积特征消费者 | 未来负荷分类，宏平均 F1 | +0.03196 | 2/3 |
| MiniROCKET 时序卷积特征消费者 | 未来负荷回归，均方根误差 | −0.00631 | 1/3 |

相对移除单流旁路，完整模型在线性分类、线性回归、时序卷积特征分类上的配对增量中位数分别为 +0.08102、+0.06619、+0.06246，同方向种子数分别为 2/3、3/3、3/3；时序卷积特征回归为 −0.00204，只有 1/3 种子为正。不能把后一项不利结果省略，也不能用应用任务代替连续演化或物理残差机制诊断。

## 安全门判定缺口

当前未提交的 `thesis_simulation_gates.py::_bypass_gate` 使用每个任务—消费者组至少 2/3 种子达标的规则。冻结 v3 原协议只规定分类劣化不超过 0.05、回归均方根误差劣化不超过 5%，未规定该门可容忍一个种子失败；不得把其他机制门的 2/3 规则自动挪用到安全门。

已重算的超阈值单元如下。这些结果保留为负面证据，不因检查点完整或其他种子良好而删除。

| 消费者 | 指标 | 随机种子 | 相对最佳单流的劣化 | 冻结阈值 |
| --- | --- | ---: | ---: | ---: |
| 固定线性消费者 | 宏平均 F1 | 29 | 0.10969 | 0.05 |
| MiniROCKET 时序卷积特征消费者 | 宏平均 F1 | 43 | 0.05563 | 0.05 |
| MiniROCKET 时序卷积特征消费者 | 均方根误差 | 43 | 20.98% | 5% |

当前结论是安全门未获得冻结协议下的完整证明。不能在看到结果后自行选择更宽松的聚合口径，也不能据此继续模型调参。待人工确认后，修正审计规则、完成剩余仿真分析；在确认前不生成“全部硬门通过”结论、不启动外层评价。

## CPU 全量测试补充

同一测试会话完成 CPU 全量测试：473 项中 458 项通过、15 项跳过、0 失败、0 错误，用时 130.29 秒。命令为 `CUDA_VISIBLE_DEVICES='' /home/wangminan/env/anaconda3/envs/chronaris/bin/python -m pytest -q --junitxml=artifacts/application_evaluation/2026-09-04_frozen-simulation-review/cpu-pytest.xml`。七项因本次显式关闭 CUDA 而跳过，另外八项为未启用的现场数据或运行环境测试；这不是 CUDA 验证，也不代表全部研究硬门通过。

测试结果 XML 位于上述被忽略产物目录，SHA-256 为 `3def6703a346657578025e78197b5e4ec0bd6d643b45954793eb53e1f9ddcc80`；`git diff --check` 通过。本次还核验两个外层 CLI：未提供 `all_hard_gates_passed=true` 的总审计结果时均拒绝启动。现有安全门测试未覆盖单个种子超阈值的边界，因此测试通过不能消除该审计缺口。

## 下一步与时间边界

本次没有修改模型、表示合同、消费者或冻结协议，只保存复核事实。等待确认的事项是无观测样本语义及安全门审计口径，不是训练是否还活着。即使恢复压力导出成功，仍需完成剩余压力场景、时间机制、总硬门和外层实验，不能承诺一个小时内完成完整论文任务。

## v3.2.3 恢复后的压力证据

用户确认后，修复已在提交 `6a2393e4` 冻结并推送，证据标签为 `evidence/thesis-v3p2p3-repair-20260904`。同一运行会话完成三随机种子、三十五场景、六方法的六百三十份压力表示，每份一百九十二个样本，验收 5/5。三个种子各有一个生理单流无观测窗口，均位于三十秒连续缺失场景，序列与池化严格为零，掩码全假，样本没有删除。

冻结消费者完成六百三十次评价、20,160 条全部可计算指标、4,032 条退化斜率和六百三十条配对统计，验收 6/6。三个无观测窗口的两类负荷消费者共六条预测保存在被忽略的 `artifacts/application_evaluation/2026-09-04_thesis-simulation-stress-consumers-v3p2p3/unobserved_predictions.csv`，总体指标保留这些窗口，不把无信息输入的预测当作有观测状态恢复能力。

零扰动核对进一步确认，压力组自身基线一致，但不等同于原干净异步输入。原干净场景的航电与生理时间戳抖动标准差分别为 2 和 5 毫秒，压力零抖动场景为 0 和 0 毫秒。将压力零抖动与零偏移、零漂移、零随机缺失、零连续缺失及 30 分贝信噪比场景分别配对，每组五百七十六项指标全部一致，最大差为零。因此压力退化曲线使用压力组自身的最小强度基线；不得将其与原干净场景的差异解释为本次表示修复的效果。

以上压力证据已完成，随后时间机制与总审计也已完成，最终判定见文末。严格安全门的三个失败单元仍保留，公开数据与鼎新外层继续关闭。

## 已完成的时间机制基线分析

时间机制表示与消费者均已完成：一百四十四份训练/验证侧表示验收 5/5，二十四个固定选择规则下的诊断消费者完成八百四十次评价、3,360 条指标和六百三十条配对统计，验收 6/6。八百四十项平均绝对误差、均方根误差与容差命中率均可计算；四百二十项相关系数因目标为常量而明确不可用，未替换为零。

结果支持连续融合表示相对朴素同步具有时间机制信息，但对深度基线的优势并不在全部种子和指标上成立。下表先对固定三十五个压力场景等权计算每个种子的平均绝对误差，再将同一种子的基线误差减去 Chronaris 误差；正值表示 Chronaris 误差更低。这是受控仿真的描述性汇总，不把场景或随机种子视作独立受试者，也不替代外层应用增量检验。

| 恢复目标 | 比较方法 | 配对误差降低中位数（秒） | 正向种子数 |
| --- | --- | ---: | ---: |
| 相对时钟偏移幅值 | 朴素时间同步 | +0.4693 | 3/3 |
| 相对时钟偏移幅值 | 跨模态变换器基线 MulT | +0.1680 | 2/3 |
| 相对时钟偏移幅值 | 连续时间变换器基线 ContiFormer | +0.0762 | 2/3 |
| 生理主响应通道时延 | 朴素时间同步 | +1.0907 | 3/3 |
| 生理主响应通道时延 | MulT | −0.4581 | 1/3 |
| 生理主响应通道时延 | ContiFormer | +0.0789 | 2/3 |

Chronaris 在种子 17、29、43 上的偏移幅值平均绝对误差分别为 0.7613、0.8137、0.9452 秒；生理主响应通道时延误差分别为 6.5634、7.7871、7.2012 秒。这里的偏移目标是幅值，不等同于有符号时钟偏移恢复；响应时延目标也限定于冻结的生理主通道。

来源为 `docs/artifacts/runs/2026-09-04_thesis-simulation-mechanism-consumers-v3p2p3/metric_long.csv`。计算顺序为筛选 `metric=mae_s`，按目标、方法、种子对三十五场景求均值，再在同目标和种子内计算方法差，最后报告三个配对差的中位数与正值个数。单场景的轨迹级配对区间保留在同目录 `paired_statistics.csv`，不合并或筛选其显著性值来制造总体结论。

无连续演化消融、物理残差和未来扰动的最终门禁见文末；本节基线结果不能替代这些消融判据。

## 压力退化与误差尾部

固定范围内的时钟偏移和漂移对下游指标影响较小，而连续缺失和随机缺失构成明显弱点。下表完整列出七个因素：先计算每个种子在组内最大与最小强度处的指标差，再取三个差值的中位数；正负方向同强度时使用既有压力统计中的等权平均。宏平均 F1 的正变化更好，均方根误差的正变化更差，两列不能用同一符号解释。

| 压力因素与范围 | 线性分类 F1 变化 | 线性回归误差变化 | 时序卷积特征分类 F1 变化 | 时序卷积特征回归误差变化 |
| --- | ---: | ---: | ---: | ---: |
| 时间戳抖动，0–100 毫秒 | −0.0143 | +0.0128 | −0.0026 | −0.0164 |
| 时钟偏移幅值，0–3 秒 | −0.0010 | +0.0003 | +0.0155 | −0.0002 |
| 时钟漂移幅值，0–250 ppm（百万分率） | +0.0048 | +0.0023 | +0.0121 | −0.0040 |
| 随机缺失率，0–50% | −0.0747 | +0.1498 | −0.1671 | +0.0179 |
| 连续缺失，0–30 秒 | −0.0736 | +2.7514 | −0.1487 | +0.1112 |
| 附加生理时延，2–30 秒 | +0.0184 | +0.0103 | −0.1070 | +0.0203 |
| 信噪比，30 降至 5 分贝 | +0.0021 | +0.0218 | −0.0873 | +0.0038 |

来源为压力消费者目录的 `stress_slopes.csv`，筛选 Chronaris、两个负荷消费者、宏平均 F1 与均方根误差后共八十四行。分类直接使用 `maximum_stress_change`，回归将该方向归一值取负恢复为原误差变化，再按因素、消费者和指标取三种子中位数。附加生理时延的组内最小强度为 2 秒，不把它误写成零时延基线。该表不对模型作重新选择，也不把每个场景当作独立样本做显著性检验。

三十秒连续缺失下的线性回归误差有明显长尾。逐样本预测独立重算与正式指标一致，下面保留全部一百九十二个窗口的统计；没有删除高误差窗口或截断预测。

| 随机种子 | 均方根误差 | 中位绝对误差 | 最大五个误差窗口的平方误差占比 |
| --- | ---: | ---: | ---: |
| 17 | 3.5960 | 0.2464 | 44.83% |
| 29 | 1.9867 | 0.2332 | 52.14% |
| 43 | 2.9884 | 0.2725 | 54.27% |

三种子的绝对误差第九十五百分位为 5.4060–8.2820，最大绝对误差为 10.9978–17.3619。因此中位误差不能代替均方根误差或掩盖长尾风险。Chronaris 在这些压力表示中没有全窗无观测样本，且本次修复已验证非空输出等价；这些退化不能归因于新增的全零无观测分支。更具体的模块归因仍需结合冻结消融结果，不能仅凭误差形态断言原因。

## 最终硬门与只读定位

受控仿真的十个编排环节全部完成或核验复用，总审计正常返回研究门未通过的退出码 2。下表对固定种子 17、29、43 使用冻结阈值；训练与评价实现检查为通过，但未来信息隔离是独立硬门，因此不能把审计文件中较窄的协议检查通过字段解释成整体协议可信或允许开放外层。

| 硬门 | 结果与依据 |
| --- | --- |
| 显式时移 | 通过；训练内均衡五分类准确率 0.2000、0.2909、0.3091，后两个种子优于随机水平和无目标候选 |
| 事件—响应配对 | 通过；训练内正确减错误配对相似度差 0.2016、0.1447、0.1950 |
| 连续演化 | 通过冻结的“偏移或时延”规则；偏移幅值恢复三个种子均优于无连续演化，响应时延三个种子均不利 |
| 物理一致性 | 通过；三个种子的有效平移与垂向残差均低于无物理约束消融 |
| 未来信息隔离 | 未通过；历史表示最大扰动为 1.9754×10⁻⁵、3.2652×10⁻⁶、2.8718×10⁻⁵，均超过 10⁻⁶ |
| 安全单流旁路 | 未通过；十二个任务—消费者—种子单元中三个超出原阈值，失败明细见前表 |

连续演化的正向证据限于偏移幅值恢复。按同种子的无连续演化误差减完整模型误差，三十五个固定场景的配对中位增益分别为 +0.3603、+0.0351、+1.0997 秒；响应时延对应为 −0.1931、−1.8705、−0.0933 秒。后者不能因前者使硬门通过而省略。物理残差比较中，每个种子的两种模型均有 6,884 个平移和 6,884 个垂向有效项，计数一致；完整模型的平移残差范围为 0.00316–0.00954，无物理约束为 0.07678–0.09987，垂向对应为 0.00647–0.02953 和 0.04288–0.14572。

未来依赖的首个变化点是事件分数，不是连续演化状态或随机误差。诊断沿用总审计的四个训练内验证窗口和 15 秒截点，只对截点之后的有效原始观测加 100,000；历史归一化输入、历史连续状态、48 维单流旁路和滞后注意力逐值不变，重复原输入输出差为零。仅末尾 16 维语义跨流支路改变；历史事件分数最大变化为 0.30448、0.06092、0.34087。种子 17 在 CPU 上也得到约 1.9752×10⁻⁵ 的历史表示变化，排除了仅由 CUDA 重复执行引起的解释。

源码解释与干预检查相互支持：`compute_vehicle_event_scores` 用全窗最大事件强度归一化，而 `_causal_semantic_sequence` 在这一操作之后才遮蔽未来。故未来幅值已经进入历史事件分数的分母，后续前缀掩码不能移除该依赖。仅在诊断内存中固定事件分数为原输入值，再输入扰动后的观测，三个 CUDA 种子及 CPU 复核的历史表示差全部恢复为零。该干预只用于定位，不是修复后候选、重新选型或新的正式结果；所有检查点哈希不变。

最小后续方向是将事件强度归一化限制在每个查询可见的历史内，并增加使用已训练检查点的回归验证；这会改变非空输入的模型行为，不属于已批准的无观测合同修复。当前不实施、不重训、不降低门槛，需先由用户确认新的修订范围。即使未来隔离修复成功，已出现的安全门失败也不会自动消失，更不能据此宣称完整论文目标完成。

原始证据位于[总审计](../../../artifacts/runs/2026-09-04_thesis-simulation-gates-v3p2p3/gate_audit.json)、[未来信息定位](../../../artifacts/runs/2026-09-04_thesis-simulation-gates-v3p2p3/future_information_diagnosis.json)和[可执行只读诊断](../../../artifacts/runs/2026-09-04_thesis-simulation-gates-v3p2p3/diagnose_future_information.py)。所有新结果的编码器与仿真评价源码冻结于 `6a2393e4190367e9504377ee2f2914926e610ae8`；诊断不读取公开数据或鼎新外层。

## 最终重放、测试与交付边界

恢复模式完整重算总审计，用时 64.88 秒；总审计、物理残差、未来扰动、连续恢复、无连续演化指标和安全门六份文件的 SHA-256 全部逐一一致，141 份表示缓存的内容、大小和修改时间均未变。失败结论可复现，不是首次执行偶然波动；[恢复证据](../../../artifacts/runs/2026-09-04_thesis-simulation-gates-v3p2p3/resume_verification.json)保留前后哈希。

最终启用 CUDA 可用环境的完整测试为 **488 项通过、8 项跳过、319 条警告、0 失败**，用时 120.85 秒。命令为 `/home/wangminan/env/anaconda3/envs/chronaris/bin/python -m pytest -q --junitxml=artifacts/application_evaluation/2026-09-04_no-observation-safety-repair/final-pytest.xml`，XML 的 SHA-256 为 `2adcdf047ffb54a3c29006fd30d3aeb834d85a2c3cd5099a3d9cdfcba815e8e5`。八项跳过为未启用的现场数据或运行环境测试。单元测试通过不能替代已训练检查点的未来隔离检查，本次真实检查点检查确实失败。

批准的修复与受控仿真分析已完成，下一步受“模型行为修订需先确认”约束而停止。公开数据与鼎新的外层分类、回归、配对任务，以及依赖最终模型的运行评估、案例和主分支合并尚未完成，不将它们改写为已完成或静默跳过。模型参数、阈值与训练次数保持冻结，未通过结论和初始失败目录保留，用户原有工作树修改未混入本次提交。
