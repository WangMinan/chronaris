# 仓库冗余与文档一致性复核

日期：2026-09-05。起点：`main` 分支 `2f31c741`。

本次先复核代码、文档和组件的冗余及文档与实现的一致性，再按用户指示直接在 `main` 清理和修复。复杂度复核采用 `ponytail-review` 的逐项格式；文档失配与功能缺陷另列，不将现有最小验证当作冗余删除。

## 复核计划

1. 盘点代码入口、组件引用、重复实现、文档导航、历史状态与断链。
2. 对候选删除追踪调用与文档引用，记录可复现依据；保留实验重放需要的协议、指标、检查点和历史来源。
3. 先形成发现清单，再实施已确认的删除、复用与文档修正；本轮不重训模型或开放真实数据外层。
4. 运行与改动相符的聚焦验证、完整测试和文档检查，记录实际净变化与剩余事项。

## 发现与处理

初始盘点覆盖 3,454 个受版本管理的路径、667 个 Python 文件、416 个 Markdown 文件。语法树扫描检出 28 组不少于十行的同构函数；调用与上下文复核后，以下项目进入本轮清理。单纯同构不等价于冗余，测试数据构造与仍有独立职责的组件不据此删除。

### 复杂度发现

- `src/chronaris/models/alignment/losses.py:L121`: delete: 旧物理辅助函数及两个未使用的特征常量；实际物理损失已委托给 `physics.py`，删除未调用实现。
- `scripts/evidence/run_weak_label_sweep.py:L50`: shrink: 五个脚本各自复制凭据读取和数据库配置组装；在现有 `access/settings.py` 中保留一套实现，保留两类既有默认地址策略。
- `src/chronaris/models/alignment/physics.py:L635`: shrink: 一阶导数和掩码 Huber 损失与 `physics_residuals.py` 重复；复用已有函数。
- `src/chronaris/representation/pretext_targets.py:L295`: shrink: 观测年龄计算在增强、预训练目标与时间合并中重复；复用现有时间合并实现，并核对缺失、同刻和空序列行为。
- `docs/STATE.md:L32`、`docs/implementation/TASKS.md:L22`: delete: 当前入口中的大段历史任务、旧指标和旧设备策略；当前页仅保留当前状态和执行顺序，历史由已有阶段报告与提交 `2f31c741` 追溯。

### 一致性与功能发现

- 24 个已迁移脚本的仓库根路径层数错误，指向 `scripts/` 或仓库父目录；导致源码导入、凭据路径和默认产物位置与文档不符。按实际目录修正，并对命令入口作统一回归检查。
- Markdown 链接初扫发现 71 处目标不存在，其中包括旧清理清单对已删除资产的历史记录；迁移产生的断链修正到已有文件，确已删除的历史资产保留说明而不伪造文件。
- 历史报告目录有失效的兼容符号链接；存在迁移后目标的修正指向，已无目标且无有效用途的删除并同步入口。
- 文档总导航、脚本与测试索引仍停留在早期布局，中期目录把六月材料称为当前事实，当前状态页又混入七月已被替代的指标；更新导航与时间范围，以九月冻结结果为当前依据。

### 不作机械删除的项目

- 当前六方法、四项消融、已冻结协议和紧凑实验数据均有实际使用或复现职责，保留；本轮源码调整不回填为旧实验源码，旧结果继续绑定证据标签 `evidence/thesis-v3p2p3-repair-20260904`。
- 19 个历史 Python 大文件超过 800 行，另有旧图表中的无调用函数。它们属于仍可调用的历史验证链，本轮优先清理不需要拆分其行为边界的独立冗余；后续若修改这些大文件，须按仓库规则按职责拆分并验证对应输出。
- 三个文档目录兼容入口是符号链接，未保存第二份正文，保留。

## 已实施的清理

- 删除七个无调用的旧物理辅助函数和两组未使用的特征常量；相对重构误差仍使用的 `_masked_mean_square` 保留。该依赖由聚焦测试确认，不按函数名或单次文本命中机械删除。
- 五个命令入口复用 `access/settings.py` 的配置解析，保留环境变量优先、按需读取文件、两类地址默认值及缺少字段时的失败行为。
- 一阶导数、掩码 Huber 损失和观测年龄复用现有实现，没有新增依赖或新的组件层；源码与命令脚本合计新增 112 行、删除 550 行，净减少 **438 行**。
- 修正 24 个命令入口的仓库根路径。新增根路径检查、异目录命令帮助验证和配置优先级测试。
- 当前状态与任务入口移除大段重复历史，原内容保存在提交 `2f31c741`，阶段结果仍由既有报告与指标承载。更新总导航、脚本、测试、目录职责、中期材料和复核入口。
- 原扫描中的 71 处失效目标，15 处是历史代码块中的原样输出，保留原记录；其余迁移断链修正或通过新的导航收敛。19 个兼容符号链接修正到受版本管理的目标，另两个只剩本地忽略资产的旧别名在核对引用后删除。

## 验证结果

- 聚焦验证最终为 **22 passed, 2 skipped**。初次验证发现相对重构误差依赖不能删除，恢复后重跑通过。
- 完整测试为 **492 passed, 8 skipped, 319 warnings**，零失败、零错误，用时 170.47 秒。命令为 `/home/wangminan/env/anaconda3/envs/chronaris/bin/python -m pytest -q --junitxml=artifacts/application_evaluation/2026-09-05_repository-cleanup/pytest.xml`；结果 XML 的 SHA-256 为 `750f60af02458fa916f1f5a6242318d88f22444af3167d716b25d1c58787e270`。
- CPU 与 CUDA 各四项原实现对照，共八项逐值等价检查通过，覆盖复用的观测年龄、导数和掩码损失；新增测试覆盖缺失特征、同刻观测与空序列。
- 49 份复用证据、六份核心审计和 27 个冻结检查点哈希全部一致，证据标签仍指向 `6a2393e4`；未运行新训练或外层评价。
- Markdown 正文链接和符号链接检查均无失效目标；历史代码块中的原始路径不作为可点击链接。新增正文的术语检查、改动 Python 文件的 Ruff 静态检查和 `git diff --check` 通过。
- 详细检查记录位于被忽略的 `artifacts/application_evaluation/2026-09-05_repository-cleanup/`；本轮修改的 Python 文件均少于 800 行。

本轮清理完成，19 个历史大文件的职责拆分仍作为后续重构事项保留。本次没有改变未来信息隔离和安全旁路失败的研究结论。

复杂度评分针对本轮采纳的源码和脚本清理，以起点版本为基准：

net: -438 lines possible.
