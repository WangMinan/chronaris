# 阶段 4 完整开发比较复核

复核目标是让完整首折、种子 17 的开发比较进入可恢复后台队列，并记录真实成本。[执行报告](../../../../artifacts/runs/2026-09-11_v4-stage4-comparison/report.md)给出方法、路线和预算；本阶段不选择正式确认配置。

计划检查四项：完整训练与验证角色和去重鼎新边界；近期适配遍历全部训练窗口与真实任务覆盖；优化器、随机状态和失败停止；训练、缓存、导出、下游拟合及完整尝试成本。实现复用现有总控、训练器、序列导出和共同下游，只增加阶段终点与完整开发模式。

针对性检查已通过：原总控、共同合同和近期模型检查 25 项通过、2 项资产选配测试跳过；新增完整矩阵与 CUDA（统一计算设备架构）末步恢复检查 2 项通过。完整回归另显式启用全部近期模型资产，包括真实 SensorLLM 分段对齐恢复，完整回归最终为 678 项通过、8 项跳过、418 项既有告警，耗时 876.23 秒；源码与测试清单均由[验证回执](../../../../artifacts/runs/2026-09-11_v4-stage4-comparison/validation_receipt.json)绑定。

审查修正了完整适配仍重复全部训练的开销：恢复重放改为倒数第二次检查点之后的一次更新；阶段 3 默认仍为两次主更新。完整适配批次保留循环成员，再补齐任务有效性，避免只保证任务存在却漏掉部分训练窗口。原有任务引导检查点额外核验各声明任务在联合适配中的有效监督次数。

本轮按用户要求只验证启动，不持续跟踪后台实验。实际完成量、失败与预算判断在后续唤醒后回填。
