# 完整开发比较的性能试验

本轮先保护已完成计算，再验证执行加速。用户于 2026 年 9 月 13 日授权：等待 CogPilot 虚拟飞行数据上 Chronaris 自监督训练的第 200 次更新完成验证并保存检查点，保留检查点后停止队列，开展独立性能试验。

## 检查范围

- 停止前验证模型、优化器、随机状态和采样位置的完整性，复制当前及最佳检查点、配置与状态，并核对散列；核验已完成单元的回执。
- 保留原运行目录与冻结代码。停止原因单独记录为用户授权的性能试验；原总控收到终止信号后写出的失败记录保留。
- 复用现有 CUDA Graph（显卡运算图重放）递推实现，固定原模型、训练样本顺序、实际批量 4、有效批量 16、浮点精度和损失。
- 从同一检查点执行相同的后续完整更新，比较序列表示、损失、梯度、参数、优化器、随机状态与采样位置；另启进程验证恢复。
- 表示的最大绝对差要求不超过 10⁻⁶；梯度及参数按既有递推测试的绝对容差 10⁻⁶、相对容差 10⁻⁵ 检查。相同加速实现的重新启动恢复要求逐值一致。报告同时保留逐值一致性和最大差，不把容差通过写成逐值一致。
- 分别记录首次捕获与后续运行成本。诊断更新不计入原队列完成量；失败与不利结果保留，未验证前不迁移生产检查点。

## 当前证据

第 175 次检查点通过 CPU（中央处理器）加载与训练状态散列校验；待第 200 次检查点写出后停止。比较工具的缺失字段、非有限值和状态变化拒绝检查通过。实际显卡试验尚未开始。

运行证据目录：`/mnt/e/chronaris-v4-results/2026-09-13-stage4-performance`。分析与结果见[试验报告](../../../../artifacts/runs/2026-09-13_v4-stage4-performance/report.md)。
