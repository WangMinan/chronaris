# 完整开发比较的执行版本迁移

本轮将已验证的显卡运算图重放接入 CogPilot 虚拟飞行数据上的 Chronaris 连续融合模型，并从第 200 次更新恢复完整开发比较。用户已授权迁移与后台恢复；保留一线程、原实际批量 4、有效批量 16、单精度、数据角色、目标、预算及确认边界。

## 检查计划

- 核验原运行目录、既有复用目录和性能试验的散列，以及普通／加速数值对照和独立进程恢复结果。
- 计算模块保持原样，只在已验证的数据域和模型入口启用执行加速。CLARE 生理数据上的九个完成模型单元保留原始逐路线结果、比较键、源码及下游来源；新回执明确说明其继承关系。
- 在新目录生成第 200 次及最佳检查点，重算执行配置对应的协议散列，并保留原协议、检查点散列和性能证据。模型、任务头、优化器、随机状态、采样位置和既有耗时不变。原训练器的恢复校验不修改。
- 保留历次尝试成本、失败记录与本次性能试验开销，不把诊断更新计为生产更新。
- 先完成完整显卡回归，再在冻结副本执行真实迁移。确认后台进程、心跳、显卡、恢复后的更新计数和新检查点。

## 当前结果

原第 200 次检查点的迁移只读验证通过，迁移前后训练状态散列一致。聚焦测试四项通过、五项因中央处理器检查环境跳过；完整显卡验证与恢复尚待执行。
