# 仿真初筛汇总与三种子复核入口

实际候选结果已接入可复核汇总，三种子复核复用同一训练器。固定初筛包含 18 个配置，实时完成情况见[当前状态](../../../../STATE.md)及对应运行清单；全体候选与 GPU 压力结果未齐，不输出进入公开开发的最终名单。

## 初筛口径

初筛以三项任务等权排名，不按窗口数加权，也不比较不同方法的预训练损失绝对值。下表明确每项主指标的固定消费者，其他消费者和机动片段、边界及延迟成绩保留在完整指标记录中。

| 任务 | 固定消费者 | 排名主指标 |
| --- | --- | --- |
| 未来负荷分类 | 线性逻辑回归 | 宏平均 F1，越高越好 |
| 未来负荷回归 | 线性岭回归 | 均方根误差，越低越好 |
| 机动逐点分类 | 因果时序卷积与持续时间解码 | 逐点宏平均 F1，越高越好 |

每条表示路线、每种方法单独排序，任务名次取平均；同分时依次按编码器参数量、实际训练时间处理。所有已规定候选结束后，汇总器才生成完整排名；八条件压力未齐时不输出进入公开开发的名单。30 秒缺失的第 95 百分位误差相对参考是否超过 20% 单独展示，单种子初筛不作最终采用判定。三种子收益、其他任务退化约束及公开数据域等权复核仍按完整计划执行。

汇总器从保存的逐窗口预测重新计算主指标，核验消费者模型、预测、表示文件的哈希，检查全部验证窗口、标签来源、训练更新数及扩展数据清单。旧 256 条轨迹结果、工程冒烟成绩或确认角色不能进入此初筛。相同预训练初始化在两条路线中以同一检查点哈希标识，监督更新和消费者更新分别记录，汇总成本时需去重共享初始化。

## 复核预算与运行接口

复核入口支持种子 17、29、43，以及独立指定自监督、任务引导路线。预训练最多 1,500 次更新，每 100 次验证，至少 500 次后按耐心五次早停；任务头预热 50 次，联合适配最多 500 次，每 50 次验证，至少 200 次后按耐心四次早停。路径中的 1,500/500 表示预算上限，实际更新数及最佳位置从检查点和运行状态读取，不将上限冒充实际训练量。

以下命令在候选与路线已按计划选定后使用；当前未启动复核，不因接口可调用而额外扩展搜索数量。

```bash
/home/wangminan/env/anaconda3/envs/chronaris/bin/python \
  scripts/evaluation/application_tasks/run_thesis_v4.py candidate-review \
  --domain simulation --method chronaris --candidate-name reference \
  --seed 29 --routes self_supervised
```

CPU 集成测试通过实际短训练验证了种子传递、仅执行指定路线、预算和早停参数、统一消费者导出、确认角色隔离。测试缩短后的更新不作为三种子研究结果。汇总测试还覆盖未完成候选、未完成压力、修改成绩与修改预测文件的拦截。

## 三种子结果与压力读取

仿真三种子结果已复用初筛预测重算与压力文件核验。读取器先检查冻结复核计划，再逐方法、候选、种子和路线读取干净结果；干净任务成绩与 30 秒缺失的第 95 百分位误差分别保存，未完成或失败的压力评价不能成为完整选型输入。正式确认角色不会进入该汇总。

仿真和公开结果共用更新预算检查，预训练与监督阶段各自验证实际记录，任务头的 50 次预热单独计入监督更新。三种子消费者指标必须保持相应种子；目录中的 1,500/500 仍只代表预算上限。压力记录同时核验候选、路线、种子、阶段、检查点和完整八条件文件，其他种子或被修改的文件会被拒绝。

复核压力评价继续使用干净消费者，先确认该单元属于冻结清单，再重算干净三项主指标与来源。单元只有干净消费者完成后才能运行；不在压力条件重新拟合。以下入口分别查看结果和执行一个已选定单元：

```bash
/home/wangminan/env/anaconda3/envs/chronaris/bin/python scripts/evaluation/application_tasks/run_thesis_v4.py simulation-review-results --domain simulation
/home/wangminan/env/anaconda3/envs/chronaris/bin/python scripts/evaluation/application_tasks/run_thesis_v4.py candidate-review-pressure --domain simulation --method chronaris --candidate-name reference --route self_supervised --seed 29
```

当前真实汇总返回等待复核计划、零个干净单元；压力入口返回显卡等待，没有启动额外 GPU 任务。合成端到端检查实际读取短训练所得的种子 29 自监督与种子 43 任务引导检查点，完成各自八条件评价，期间禁止消费者拟合；工程测试的三次训练更新明确不作为研究预算通过证据。研究预算边界、错误种子、文件变更与压力失败状态由独立检查覆盖。

仿真与公开三种子成绩的单因素采用判据已接入下述入口；最终组合及冻结仍待后续完成。单个结果读取入口不自动把某项改善认定为最终采用收益。

## 公开确认池数据准备

两套公开确认池已完成原生窗口准备和五折角色核验：CogPilot 虚拟飞行数据为 28 人、9,173 个窗口，其中难度分类 3,758 个、事件响应 5,415 个；CLARE 认知负荷数据为 15 人、3,068 个窗口。开发受试者全部排除，每个窗口只分配到一个外层留出折；未产生模型确认成绩。CogPilot 准备约 448.75 秒，CLARE 约 102.02 秒，原始采样点未降采样。

两个独立 CPU 进程已开始按公开外层五折预拟合内部训练部分的输入归一化，验证和留出窗口由数据提供器直接拒绝。拟合参数使用现有共享归一化与物理校准工具，完成后缓存供各方法和种子共用；启动记录见 `normalization_launch.json`，实时进展在记录所指运行目录。

本批端到端与相关读取检查 20 项通过、1 项 CUDA 检查跳过，另有尾部与分派检查 2 项通过；完整 CPU 回归 593 项通过、30 项跳过，用时 136.03 秒。日志与来源哈希追加至[验证摘要](validation_summary.json)，[真实复核读取](simulation_review_readiness.json)保持等待、零个干净单元。

## 跨域单因素采用判断

采用入口重新读取已核验的仿真及公开三种子结果，要求两者来自同一冻结复核计划，并覆盖五种可训练方法的两条路线。任一任务、种子或压力成绩未完成时，入口保留等待；执行失败则明确阻断，不输出部分推荐。当前真实执行为等待完整三种子复核，零个方法与路线推荐，尚未冻结配置。

任务收益和其他任务退化按三个种子的等权均值检查，至少两个种子需同方向改善。分类收益阈值为宏平均 F1 增加 0.01，回归为误差降低 2%；其他任务最大允许 F1 降低 0.02 或回归误差增加 5%。长缺失第 95 百分位误差逐种子比较，每个种子均不得超过参考的 1.2 倍。全部逐种子值、方向计数和通过情况保留，参考误差为零时相对比例记为不适用。实施口径已在可训练候选三种子复核成绩产生前写入主计划。

推荐仍遵循原有跨域名次排序：先任务内排名、再数据域等权、最后取三个种子名次的中位数；只在通过采用条件的配置及参考中选择排名最前者。某项达到收益阈值不保证替换整体排名更好的参考。同分比较各数据域平均模型参数量之和，再比较实际训练耗时；参数量不因公开数据多跑几个折而重复放大。

```bash
/home/wangminan/env/anaconda3/envs/chronaris/bin/python scripts/evaluation/application_tasks/run_thesis_v4.py candidate-adoption
```

结果写入复核目录的 `adoption_decisions.json`，包含来源摘要哈希和逐项判断。该文件只提出单因素推荐，组合评价、公共目标给基线的同等比较机会和最终配置冻结继续按主计划完成。

采用判断聚焦检查 4 项通过，完整 CPU 回归 597 项通过、30 项跳过，用时 143.69 秒。来源和日志见同一验证摘要；[实际采用入口](adoption_readiness.json)保持等待，零个推荐，配置未冻结。
