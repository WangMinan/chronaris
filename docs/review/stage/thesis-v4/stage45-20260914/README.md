# 阶段 4.5 建议与实现复核

本轮复核远端 GPT PRO 的阶段 4.5 建议，目标是确认其源码与结果依据，并完成可独立运行的有界开发队列。审阅起点为 `5b48a0cc`，四个指定会话均已读取，阶段 4 的 25 个单元和 43 条路线已结束。

## 主要发现

入口配置差异、余弦缩放、分支压缩、任务头与共同下游选择不一致，以及加速仅覆盖 CogPilot 的判断均成立。原 v4 原生数据采用空运动学关系校准，仅提供观测重建锚定；因此“完整机制参考”必须逐项说明活动目标，不能直接称为完整物理方法。最佳检查点已经正确导出，门偏置也不能单独证明门无法开启。

实现复用共同评价入口、已有注意力和保真候选、冻结表示导出、原生分组下游及进程总控。新增[阶段 4.5 方案](../../../../requirements/thesis-stage45-development-20260914.md)固定候选、诊断、开发设置、选择规则、数值容差和 72 小时上限。所有阶段到开发报告为止。

## 检查计划与结果

检查覆盖旧入口默认行为、单因素配置差异、数据角色、候选覆盖、负面结果保留、到限停止、真实机制短入口、分支诊断和独立恢复。针对性检查已完成 26 项通过、1 项显式真实入口跳过；真实入口和完整显卡回归随后单独执行，最终凭证见[运行报告](../../../../artifacts/runs/2026-09-14_v4-stage45/report.md)。

首次真实入口测试因测试临时目录的父目录不存在而在 setup 阶段失败，未执行训练；失败日志保留于 `/mnt/e/chronaris-v4-results/stage45-entry-check.log`，修正目录后在新目录重新验证。

研究结果尚未生成。编码完成、工程验证及后台启动分别记录，不作为候选收益或历史机制门通过的证据。

独立读者检查发现复核汇总可能混入同名学习率基线，已限定 Chronaris 方法并增加重复任务拦截。文档明确 72 小时不保证全部完成；预算耗尽后不自动重置时间。

一次并行工程性能检查遇到汇总代码修改，触发共同合同的源码变更保护；后续检查在补齐逐单元冻结合同检查前主动停止，所有日志及停止回执保留于 `/mnt/e/chronaris-v4-results/2026-09-14-stage45-validation/`。稳定源码上重新验证，失败不覆盖。

最终完整显卡回归为 688 项通过、21 项跳过，源码与测试清单指纹验证通过。CLARE 图资格两项未通过及独立恢复两项通过分别记录，不用工程回归改写资格结果。
