# 完整开发比较收口复核

2026-09-14。本轮完成阶段 4 的结果与真实成本验收；正式配置冻结和新训练属于阶段 5。

## 复核计划

1. 从已结束的冻结运行读取 25 个模型单元、43 条表示路线，核验来源、共同任务合同、下游结果、恢复与学习记录。
2. 修复强制终止缺少尝试收据时的成本漏计，分别报告已记录耗时、检查点可证的历史投入下界、恢复后耗时及新运行估算；保留未计时尾段的不确定性。
3. 完整报告所有方法和监督路线的有利与不利结果；以同路线单流参照衡量 Chronaris，窗口表示仅参加共同线性下游。
4. 按已测单元列出方法、任务、路线、折和种子的预算情景，明确正式矩阵未冻结。
5. 在独立输出目录实跑收口入口，执行针对性测试和文档检查，确认原运行文件不变，再提交推送。

## 结果

已完成，下列记录对应本轮真实验收。

2026-09-14 完成复核。[验收报告](../../../../artifacts/runs/2026-09-14_v4-stage4-closeout/report.md)与[结果分析](../../../../artifacts/runs/2026-09-14_v4-stage4-closeout/analysis.md)已生成；25 单元、43 路线、65 组下游、1075 个来源文件通过独立读取和散列核验。训练与验证角色、相同目标、全验证窗口保留、分组指标汇总及训练／恢复收据通过检查。

成本汇总的根因是被停止的旧单元没有写出尝试收据。共享 `attempt_accounting` 保留原收据总和，并仅在该迁移单元缺少父级尝试收据时补入检查点可证的训练前缀，避免重复计算；历史下界补入 24.55 小时。旧启动和未落盘尾段仍未完整计时，不声称恢复了精确总成本。

收口入口为 `scripts/evaluation/application_tasks/close_stage4_comparison.py`，只向新的独立目录写出产物；训练结果与原来源版本均未改。第一次报告预检发现朴素同步的完成字段为 `training_status`，已按现有格式处理并实跑通过；没有修改实验完成标志。报告不生成同监督混合排名，自监督和任务引导分别成表。

验证为 11 项通过、4 项显式基线入口测试跳过；覆盖成本补计／去重、非法耗时拒绝、分组加权汇总、非有限学习记录拒绝，以及既有队列失败恢复与迁移合同。两条既有岭回归病态矩阵警告和全部不利成绩保留。正式确认未打开，后续矩阵和预算是阶段 5 的待冻结输入。
