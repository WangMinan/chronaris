# 冻结编码器后的原生留出评价

公开数据和鼎新的冻结编码器评价步骤已接入共享训练产物、表示导出与固定消费者。合成输入上的两域双路线实跑通过，实际正式成绩仍待候选选择和配置冻结后生成。

公开评价读取确认池的内部训练、验证与外层留出角色，编码器检查点必须与该折及数据清单一致。新逻辑回归、岭回归和 MiniROCKET（时间卷积特征消费者）只拟合训练表示，使用验证受试者选参，最后输出留出受试者成绩。

鼎新在已选检查点通过完整性、数据角色和初始化来源核验后，复用完整训练架次目标重拟合。最终消费者训练角色包含原内部训练、验证和隔离上下文；编码器权重及其内部训练归一化保持冻结。分类和回归消费者继续采用固定参数，多视图权重与逐架次统计沿用已有实现。

导出复用同一个带掩码池化函数，开发学习曲线原有重复实现已删除。缓存检查覆盖样本顺序、检查点、方法、折、导出角色和标签来源；恢复时无需重新编码。表示写入与消费者训练继续使用已有模块，没有新增训练器。

本步骤显式区分配置选择使用验证标签、编码器检查点选择使用验证标签、编码器反传使用标签三种来源。正式运行要求 RTX 4090、CUDA 检查点和固定消费者参数；CPU 合成测试必须显式标记工程用途。新模块没有单独开放正式 CLI（命令行入口）；全局配置冻结及正式训练编排尚待接入后调用。

集成检查为 5 项通过、1 项 CUDA 检查跳过：包括两域各一次两更新预训练、一更新任务头预热及一更新联合适配，两条路线重新拟合消费者；另覆盖既有学习曲线、候选复核和压力导出。检查点哈希或初始化文件变化、缓存标签来源变化均被拒绝。完整 CPU 回归 559 项通过、29 项跳过，用时 116.64 秒，见[验证摘要](validation_summary.json)。初次测试遗漏必需字段参数及鼎新测试目标长度未同步扩展的失败均保留，生产来源检查未放宽。

源码入口为 `src/chronaris/evaluation/application_tasks/v4_native_frozen_evaluation.py`；原始日志位于 `artifacts/application_evaluation/2026-09-08_v4-native-frozen-evaluation/`。当前 GPU 初筛继续使用独立冻结工作树。

验证前向复用对照另增 CUDA 用例，等待当前 GPU 队列结束后执行；CPU 用例通过，GPU 用例当前跳过。

## 后续 GPU 接续运行

候选压力队列已从冻结提交 `87afc941` 启动，当前等待正在运行的初筛队列释放同一 GPU 锁。[启动记录](pressure_queue_launch.json)固定进程、工作树和编排脚本哈希。接续先运行完整 CUDA 回归（包括新增验证前向复用对照），通过后才逐个评价已完成候选的两条路线和八种条件；失败保留，不重拟合压力消费者。预计最多 36 个候选路线单元，实际数量取决于初筛成功单元。

等待队列 PID 为 `5419`，当前无 GPU 子进程；其 30 秒状态写入 `artifacts/application_evaluation/2026-09-08_v4-candidate-pressure/queue_state.json`。初筛源码与进程保持原样。队列完成后使用已有汇总器重算候选排名；正式配置选择仍只依据开发角色。
