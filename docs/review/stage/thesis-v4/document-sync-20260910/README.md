# 融合表示下游评价文档同步复核

日期：2026-09-10。范围为文档同步、过时入口清理、检查与提交推送；不修改源码、模型配置、运行结果或冻结工作树，不启动训练。

## 范围与计划

先重新读取 Codex 任务 `01a07065-99d0-7a91-a8a0-1a7ef7499791` 和 ChatGPT 会话 `6a9b93d9-83d4-83e9-8b5b-d3a52c0b0e74`，再按当前会话的最新确认校正文档。用户已选择潜在特征可用于重新训练下游算法的评价方式；无需取得另一组固定算法接口，也不以新增鼎新数据或业务标签为开发前提。

本轮核对当前任务、状态、需求和产物入口，以及方法注册、任务目标、下游拟合、导出池化与队列失败路径。编辑后检查链接、术语、状态一致性、历史证据保留和读者可理解性，最后提交推送。

## 发现与处理

现有共同下游评价结构与用户确认一致，文档需要纠正的是当前入口、任务解释和未实现范围。下表记录具体处理，支撑“目标已同步、代码阶段仍待执行”的判断。

| 发现 | 本轮处理 |
| --- | --- |
| 文档导航仍将 v3.2.3 称为当前合同 | 新方案成为当前入口，旧 v3 和七月规格改为历史来源 |
| 任务与复核索引反复出现旧启动、等待和持续运行状态 | 收敛为最新任务清单与按职责组织的证据索引，历史状态回到对应日期报告 |
| 旧规则要求从主分支开始或自动推进全部实验 | 明确继续当前研究分支，先文档交付；后续修复与适配不等于恢复旧全部矩阵 |
| 鼎新仍有两架次、旧划分和未通过隔离的当前表述 | 指向已通过的单记录时间块修订；旧设计作为沿革保留 |
| 机动强度分类被表述为直接回应动作分类 | 改为运动强度组件验证，不等同于具体机动动作识别 |
| 生理字段预测与真实压力、精神状态任务易混淆 | 新方案明确目标来源、可支持的结论及业务标签缺失 |
| 近期模型可能被误认为已接入或改用原生预测即可完成 | 明确 TimeCMA、Chronos-2 必做接入验证，SensorLLM 尽量适配；共同下游表示、监督和资源尚需实测 |
| 七月表示合同混合接口与旧训练设计 | 保留现有六方法接口来源，注明混合窗口、原生时间和训练日程的继承关系 |

工作区原有未跟踪的 `thesis-downstream-representation-plan-20260909.md` 草稿已读取并保留，完善同步日期后纳入文档交付。文件名继续使用讨论日期，未另建重复方案。原任务页与复核入口逐条流水记录可在提交 `986663e7` 追溯；实际报告和指标文件未删除。

## 代码与运行事实核对

- `pretraining_encoders.py` 和 `v4_final_reporting.py` 仍只覆盖原方法集合，近期方法尚未接入。
- `v4_grouped_consumers.py` 已按各表示分别拟合下游模型；鼎新固定线性算法、公开线性与时序卷积特征补充的范围已写入方案。
- `application_finetuning_export.py` 先复制序列到中央处理器再池化，`common_pretraining.py` 的适配器在显卡上池化；本轮仅确认差异，未修复。
- `v4_pipeline_steps.py` 压力阶段异常未同步写入子队列终态；本轮仅更新任务和说明，未改运行状态文件。
- 复查进程 82623 不存在，原 `pipeline_state.json` 仍为失败状态；旧完整显卡测试 650 通过、8 跳过和 9 月 9 日聚焦测试 12 通过均是历史验收，不写成本轮新跑测试。

## 验证与剩余工作

本轮检查通过：16 个变更文件均为 Markdown 文档；232 个本地链接无缺失，差异空白检查通过。读者可见术语扫描仅命中旧规格中解释历史机器任务标识的一行，未发现将内部代号作为新任务名称的残留。当前导航中的旧持续运行、当前 v3 合同、回到主分支开发和机动动作等同表述已清理。

独立读者仅凭新方案、状态、任务、需求与总导航，正确回答研究问题、下游训练方式、标签依赖、三个模型状态、后台与本轮范围、历史继承和下一步工作，未发现跨页矛盾或无依据的已完成结论。

源码指纹仍为 `75254f7454e2d16105052f27ace805edf03b48584fd0030551bf220411b1ce93`；原运行两份完成收据散列核验通过，初筛 26 个配置与压力 9 个路线单元记录一致。未修改原始报告、指标、配置或检查点，本轮不重跑模型测试。

新增方案中的方法接口、监督分配和完整运行预算属于后续编码实测项，文档同步不将其标记为完成。下一步按[执行清单](../../../../implementation/TASKS.md)修复一致性和失败终态，再进行产物兼容核验及近期模型适配。
