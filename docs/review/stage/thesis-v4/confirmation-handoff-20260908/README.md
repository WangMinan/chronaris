# 正式训练自动接续与鼎新去重

正式原生训练已复用现有预训练、任务适配、带掩码导出及固定消费者，补齐冻结配置校验和整批执行。两条路线相同配置共享自监督初始化；固定网格基线的投影与消费者结果也共享，神经更新计为零。公开确认使用已完成的十个内部训练归一化缓存，训练提供器拒绝留出样本。

冻结文件绑定开发选型、三种子压力、数据清单、源码、测试集和完整 CUDA（统一计算设备架构）测试收据。采用公共增强或多时域目标而基线尚未比较、连续状态触发新增单元条件、或条件组合仍待评价时，入口明确等待，不能自动跳过科学比较。

实际开发自动链已从 `dce2625e` 冻结工作树启动，进程 46355 等待初筛压力，随后接续公开首折、三种子完整开发、复核压力和采用判断。当前源码的正式接续链尚待最终提交和冻结启动；它先等待开发结论，再完成当前 CUDA 全量测试与配置冻结，随后并行运行一条 GPU 神经队列和一条 CPU 固定基线队列。每个单元以独立子进程释放设备资源，已有完成单元恢复时跳过。

鼎新已按用户 2026-09-08 的明确指示修改评价范围：保留一段航电对应的完整记录，采用有时间隔离的单记录评价。具体选择规则、实际覆盖及重复来源抽查见[去重报告](../../../../artifacts/runs/2026-09-08_v4-dingxin-deduplicated/report.md)。新范围为公开 360、鼎新 36、仿真 36，共 432 个方法—划分—种子—表示族评价单元；原 468 单元计划及重复两架次的失败证据保持原样。鼎新结果只能支持单记录时间块上的描述性比较。

本批训练、评估、恢复、哈希拦截、CPU/GPU 编排和去重隔离通过完整 CPU 回归：**607 项通过、30 项跳过，136.67 秒**。首次全量检查只有一处旧错误提示断言未同步，修正后完整复验通过，失败日志保留于[验证记录](../../../../artifacts/runs/2026-09-08_v4-dingxin-deduplicated/validation_summary.json)。当前 GPU 被研究初筛占用，尚未对本批运行完整 CUDA 测试。新增代码未产生正式确认成绩。

复现入口为 `scripts/evaluation/application_tasks/run_thesis_v4.py` 的 `dingxin-retained-data`、`configuration-cuda-validation`、`freeze-configuration`、`native-confirmation-cohort` 阶段。两个自动接续脚本仅负责调用这些入口。仿真主表生成与 35 场景、两种整模态缺失现已接通，见[完整主表入口](../../../../artifacts/runs/2026-09-08_v4-formal-mainline/report.md)；核心消融与论文材料仍在推进，不能以入口完成代替完整实验完成。
