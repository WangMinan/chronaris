# 阶段 4 参数分派修复与恢复复核

本轮修复共同开发入口向非 Chronaris 方法传入专用融合参数的问题，保留既有训练预算、数据角色与下游评价合同。9 月 12 日 09:09 的失败发生在生理单流基线训练之前；此前 CLARE 生理数据上的 Chronaris 两条路线已完成，共 550 次更新、4 份检查点及 4 组下游模型。

复核计划包含三个闭环：四种可训练基线通过真实 CLARE 小样本的训练、导出及双路线下游；原运行源码与新源码的计算路径一致性审查；已完成单元的检查点重载、固定批次重新编码、全部归档和全部下游预测的核验。只有明确的参数分派修复及恢复编排变化获准复用，模型、损失、数据、预算或下游规则变化均拒绝继承。

恢复使用新运行目录和冻结源码副本。旧失败记录与所有原文件按散列绑定，已完成训练不重复；新的复核和拟合耗时、原单元耗时及失败尝试耗时分别保留。新队列继续原来的 25 个模型单元和 43 条表示路线，正式确认保持关闭。

验证结果和启动凭证在[恢复报告](../../../../artifacts/runs/2026-09-12_v4-stage4-recovery/report.md)回填。

针对性检查已通过：总控、完整适配恢复与来源差异拒绝检查为 8 项通过、4 项实际数据选配检查跳过；显式启用真实基线后 7 项通过，四种基线共完成 24 次真实更新。第一次测试目录准备失败，未进入模型执行，日志独立保留；调整到本地临时目录后完成全部基线验证。

全量验证首次调用暴露了新增测试对显卡锁的二次申请：完整验证器已在父进程持有显卡锁。已将测试限定为复用该保留，实际训练器、导出、下游及生产锁均保持原路径；首份失败验证回执保留，重新执行完整 CUDA 回归。
