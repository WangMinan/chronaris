# 阶段 4.5 暂停复核与恢复

2026-09-14，用户授权立即采用最近落盘检查点暂停，完成能力与性能复核、编码和验证后重新启动后台队列。用户取消本轮 72 小时硬上限；候选矩阵、任务、数据边界与数值判据保持明确，阶段 5 继续关闭。

## 现场与计划

原总控 445416 和训练子进程 453545 已停止。第 75 次更新的模型、公共任务头、时移头、优化器、随机状态及采样位置完整保留；暂停前内存约到第 90 次，后 15 次未落盘更新不作为可恢复成果。停止回执及字节校验保存在 `/mnt/e/chronaris-v4-results/2026-09-14-stage45-pause-review/`。原目录和全部资格失败结果保留。

本轮先复用既有检查点对照工具，增加原生数据域及机制头状态支持，记录完整更新的分段时间。对同一第 75 次状态比较普通执行与显卡运算图重放，验证输出、损失、梯度、参数、优化器、随机状态和采样顺序；独立进程恢复要求逐值一致。仅采用通过原数值判据且具有实测收益的执行方式。

能力复核使用现有合法开发快照，沿用训练、验证角色和共同线性下游；不将预训练损失改善写成业务效果。已有分支诊断支持单流保真候选，本轮不无界添加模型结构。

恢复代码须保留已完成的 34 个编排步骤证据，复用其中 32 个诊断步骤及已落盘训练，重新执行来源核验与计划，不重复运行全部分支探针和资格比较；新的执行与报告绑定旧来源清单。取消 72 小时自动截断，补齐失败成本与恢复记录，完成验证后启动，随后结束会话等用户手动唤醒。

## 结果

同一第 75 次状态继续第 76、77 次更新，普通执行核心耗时 184.54 秒，显卡运算图核心耗时 20.21 秒，约加速 9.13 倍。输出最大差 5.96×10⁻⁷，参数最大差 2.98×10⁻⁸，原容差通过；独立进程恢复逐值一致。该结果只支持当前预训练状态的执行迁移，不覆盖冷启动、其他候选或任务引导新优化器。

单个普通执行微批次的算子剖析记录约 123 万次普通内核启动，说明小算子调度是重要成本；剖析本身产生显著开销，其耗时不纳入加速比。Nsight 工具在工作负载启动前连接超时，已保留日志并用 PyTorch 剖析完成取证。

真实共同入口的短程检查已经完成图执行预训练、普通执行任务引导微调与导出，初始化权重保持一致，4 项检查通过。完整状态迁移入口、能力快照和最终显卡回归的结果见[运行报告](../../../../artifacts/runs/2026-09-14_v4-stage45-pause-review/report.md)。
