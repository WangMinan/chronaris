# Chronaris 当前任务

更新时间：2026-09-08

## 正在执行：v4 双路线开发与实验

用户已批准[完整 v4 合同](../requirements/thesis-v4-development-plan.md)，并要求 Goal 模式持续推进、不在阶段门禁请求确认。分支为 `codex/thesis-v4-recovery-20260905`；旧研究证据保留，正确性失败修复后继续，效果失败如实记录。

- [x] 建立分支，单独提交评审与诊断材料（`c79aecb2`）。
- [x] 因果归一化、独立掩码、短窗口增强与物理观测锚定；旧权重三种子历史隔离通过，新锚定 CUDA 十更新冒烟通过。
- [x] 自监督更新预算、梯度累积恢复、微调任务头与鼎新显式内部目标拟合；十次完整 CUDA 更新冒烟通过。
- [x] 公共模态目标与微调双阶段更新日程；模拟观测的同初始化双路线 CUDA 冒烟及历史隔离通过。
- [x] 四域双路线 CUDA 冒烟、标量固定消费者闭环、全量开发归一化与完整测试（524 通过、8 跳过）。
- [x] 新仿真参数及上下文清单冻结，256 条训练和 64 条开发干净轨迹生成完成。
- [x] 公开固定开发组全部合法窗口、三折与确认池清单、原生缓存和分层平衡采样。
- [x] 五方法双路线干净 500 更新诊断及 50/200/500 消费者曲线，6,447 次神经更新含下游消费者。
- [x] 八条件实际压力、Chronaris 物理与注意力、状态范数及长尾分解，80 个评价单元全部完成。
- [ ] 汇总训练诊断图表，并按原预算推进公开与鼎新开发复核。
- [x] 公开受试者选参与汇总、鼎新共享上下文权重和逐字段消费者，六个真实工程组合完成读写验证；接入完整开发诊断入口。
- [x] 一次训练扩展至 512 条轨迹、64 个参数档案；原有 320 条训练与开发轨迹的文件哈希及张量不变。
- [ ] 九候选、公开开发复核和两路线冻结。
- [x] 两种注意力、关闭同刻目标、独立原生历史配对，CPU/CUDA 恢复及微调导出检查通过；完整测试 554/8。
- [x] 扩大缺失覆盖与实际多时域预测，CPU/CUDA 恢复和微调导出通过；完整测试 559/8。
- [x] 单流保真、质量门、固定候选初筛编排及有效状态非有限检查；完整测试 570/8。
- [ ] 18 个扩展清单初筛单元实际运行、开发压力、分组选型及公开复核。
- [x] 用户要求的冗余复核、CPU 提前拟合及部分恢复、验证前向复用；CPU 回归 550/29，CUDA 对照待空闲窗口。
- [x] CPU 压力实际重放核验：序列最大差 `1.907e-6` 超阈值，保留失败并停止该推理队列。
- [x] 两套公开确认池的 CPU 原生窗口准备：CogPilot 28 人/9,173 窗口，CLARE 15 人/3,068 窗口；五折角色检查通过。
- [x] 实际初筛结果重算、文件与角色核验，三种子独立路线复核预算入口及 CPU 集成测试。
- [ ] 公开确认池十个内部训练归一化缓存；两个 CPU 进程已与 GPU 初筛并行启动。
- [ ] 完成初筛及公开开发选型后，执行三种子复核；当前未扩大搜索。
- [ ] 正式确认、压力、消融、中文证据材料与复现交付。

初始五方法双路线学习曲线和八条件压力已完成，后续九候选相对扩展清单下重新训练的修复参考比较。公开与鼎新按原计划进入开发复核；已有十更新工程测量与完整输入接口不额外扩展成新的五方法五百更新搜索矩阵。尚无 v4 正式确认成绩；运行、失败与成本见 [v4 实施复核](../review/stage/thesis-v4/README.md)。下方 v3 任务为历史记录，其阶段确认规则由本轮授权与合同替代。

## 已完成：主分支冗余清理与文档一致性修复

已按用户要求先复核，再直接在 `main` 完成清理和修复；完整测试 `492 passed, 8 skipped`。源码和脚本净减少 438 行，24 个入口根路径修正，文档正文链接与符号链接检查通过。复核范围、发现与剩余历史大文件事项见[清理复核](../review/stage/thesis-mainline/redundancy-and-docs-review-2026-09-05.md)，当前实验事实以[状态页](../STATE.md)为准。

本轮已按以下顺序收口，未触发新的训练或外层评价：

1. 检查组件调用、重复实现、脚本路径、文档断链和过时的执行指令。
2. 删除确认未调用的旧辅助代码，复用现有同职责实现，修正脚本迁移后的路径。
3. 收敛当前文档，修复历史入口，将历史快照与当前事实分开。
4. 完成聚焦验证、完整测试、文档术语与链接检查，并记录实际净变化。

## 后续研究任务

以下工作须单独确定修订协议和评价范围，再从 `main` 开展；本次清理不改变已冻结模型行为。

1. 修复事件分数全窗归一化造成的未来依赖，并使用已训练检查点验证历史表示隔离。
2. 分析安全旁路的三个失败单元与连续缺失下的误差长尾，确定新的训练内验证方案。
3. 研究机制门与外层授权满足后，才安排公开数据与鼎新的冻结外层确认，以及相应案例和运行评估。

## 已完成阶段与唯一事实源

已完成研究基线包括可信训练修复、原生时间输入、可学习事件语义、候选冻结、常规仿真、四项消融、压力与时间机制评价。阶段完成量和结果统一从以下入口读取：

- [当前状态](../STATE.md)
- [冻结清单 v3.2.3](../requirements/thesis-frozen-paper-evaluation-v3.2.3.md)
- [实验复核](../review/stage/thesis-mainline/frozen-simulation-review-2026-09-04.md)
- [主分支集成复核](../review/stage/thesis-mainline/main-integration-2026-09-05.md)
- [产物索引](../artifacts/ARTIFACTS.md)

七月任务与设备迁移安排属于历史协议，不是当前执行指令。其完整文本可从 Git 提交 `2f31c741` 的 `docs/implementation/TASKS.md` 追溯；相关研究报告仍由[历史笔记](notes/README.md)与产物索引导航。

## 执行约定

使用 `/home/wangminan/env/anaconda3/envs/chronaris/bin/python`。当前论文级仿真必须使用 CUDA（统一计算设备架构）及可用显卡；不回退到旧协议的 CPU 长跑策略。原始大数据、检查点、逐样本预测与运行日志继续保存在被忽略的 `artifacts/`，紧凑证据位于 `docs/artifacts/runs/`。不依赖新增鼎新一手数据或专家标签。
