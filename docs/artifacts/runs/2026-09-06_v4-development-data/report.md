# v4 开发数据与分组合同

固定受试者的公开开发窗口已全部保留，并完成原生点数、信号和任务覆盖检查。下表用于核对实际输入规模；不包含编码器训练或任务成绩。

| 数据 | 受试者 | 合法窗口 | 任务覆盖 | 数据整理耗时 |
| --- | ---: | ---: | --- | ---: |
| CogPilot 虚拟飞行 | 7 | 2,524 | 难度 1,062；离线事件条件响应 1,462 | 244.27 秒 |
| CLARE 认知负荷 | 4 | 794 | 每窗均有负荷二分类与连续评分 | 41.71 秒 |
| 新仿真训练 | 32 个参数档案 | 1,024 | 256 条轨迹各四个上下文 | 与开发合计读取 6.12 秒 |
| 新仿真开发 | 8 个参数档案 | 256 | 64 条轨迹各四个上下文 | 同上 |

CogPilot 生理与航电各窗均有观测，生理有效点数为 7,555–34,466。CLARE 保留单流无观测窗口，不因模型适配删除样本；受试者 1390、1629 分别有 1、7 个生理无观测窗口，1624、1629 分别有 5、38 个第二流无观测窗口。具体覆盖见两个 `summary.json` 文件。

公开划分由固定哈希顺序生成，开发三折和确认五折的受试者互不交叉；训练随机种子不改变数据角色。确认五折内的验证受试者也预先固定。公开确认观测尚未读取，新仿真 512 个确认上下文仅保留身份清单。历史使用清单扫描 213 个历史公开数据检查点，核对训练、验证及归一化拟合角色，确认至少 16 名 CogPilot 与 16 名 CLARE 受试者曾参与开发；其余均标为未证明从未开发，不生成“未开发子集”附表。清单为 `historical_public_usage.json`，当前证据类别统一为冻结后的分组评价。

共享训练器按受试者、运行或实验段分层平衡采样，并保存样本游标用于完整更新重放。同域不同任务窗口存在部分标签时，任务损失按有效批量内的真实有效样本数归一化，再等权汇总任务；不同微批次的标签覆盖不会改变任务权重。

缓存复用整份原生记录及合法窗口，内容摘要进入样本和准备清单；缓存被改写时拒绝复用。准备缓存读取实测 CogPilot 19.99 秒（首次校验全部文件），CLARE 0.72 秒。无降采样。分组、原生窗口和缓存篡改检查为 11 项通过；平衡采样、两条更新路径及任务头聚焦验证为 9 项通过。本批完整测试为 521 项通过、8 项跳过，用时 440.06 秒；摘要与源码指纹见 `validation_summary.json`。

复现入口：`scripts/evaluation/application_tasks/run_thesis_v4.py public-data --domain cogpilot`，CLARE 将数据名改为 `clare`。公开受试者冻结清单为 `docs/requirements/thesis-v4-public-subjects.json`；新仿真使用既有 v4 冻结参数清单。两条路线均使用同一数据合同。

鼎新原生输入已接入：前 30 秒保留过去运动学字段，两个外层折的内部完整支撑分组仍为每架次 18/6/6 个航电上下文。对应视图数分别为 18/6/6 和 36/12/12；内部任务头仅接收训练与验证目标，分别保留 12、11 个生理字段。外层与隔离输入由开发读取器显式拒绝，尚未生成正式消费者结果。
