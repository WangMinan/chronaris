# 一次训练规模扩展验收

训练规模已按预先冻结的预算判据从 256 条扩展到 512 条轨迹，新增 32 个参数档案、每档案 8 条轨迹。开发集仍为 64 条轨迹，确认仅保留原来的 128 条轨迹清单；没有生成确认观测。

扩展在初始五方法双路线诊断全部完成之后执行。入口重新核对十个训练单元的更新数、30 个阶段消费者完成状态，以及触发曲线第 200、400、500 次更新的损失和原检查点哈希；未依据压力结果或确认成绩改变扩展条件。

新根完整复现原 320 条训练与开发轨迹的原生观测和真值文件哈希。加载后再次比较原有全部上下文，输入数值、时间戳、有效掩码、观测年龄及来源哈希逐值相同，原训练集合严格包含于扩展后的训练集合。新增档案使用原冻结清单中的预留身份和种子，没有增加原档案的重复轨迹。

下表展示当前统一数据角色，支撑后续候选共享相同训练清单的判断。

| 数据角色 | 参数档案 | 轨迹 | 30 秒上下文 |
| --- | ---: | ---: | ---: |
| 训练 | 64 | 512 | 2,048 |
| 开发验证 | 8 | 64 | 256 |
| 确认清单 | 16 | 128 | 512，观测未生成 |

生成耗时 87.27 秒，身份隔离、有限观测、状态覆盖和事件数量检查通过。扩展数据使用独立划分标识，数据指纹为 `63179d4e7913a537a89094d8e73361d06333a2d99139f6c04aac5c440b6c7d00`，避免误用初始 256 条轨迹拟合的归一化和检查点。[生成验收](v4_generation_audit.json)、[激活合同](extension_contract.json)和[全量加载核验](loader_audit.json)保留具体证据。

聚焦检查为 **4 项通过、1 项 CUDA（统一计算设备架构）用例跳过**，覆盖扩展判据、矩阵未完成拦截、数据角色与共享诊断入口；随后完成真实 2,304 个训练和开发窗口的加载检查，见[验证摘要](validation_summary.json)。本次未新增神经网络训练。

复现入口为指定环境下的 `scripts/evaluation/application_tasks/run_thesis_v4.py expand-training --domain simulation`。重型数据位于 `artifacts/application_evaluation/2026-09-07_thesis-v4-simulation-expanded/`。后续仿真候选、复核与确认训练均使用这一清单；原 256 条轨迹的初始诊断结果保留为预算诊断，不与扩展后的候选成绩拼接为同一训练曲线。
