# 原生连续状态 CUDA 图执行对照

原生 CogPilot 四个完整 30 秒窗口的前后向代表性耗时由 51.94 秒降至 4.22 秒，约提速 12.32 倍。CUDA（统一计算设备架构）图只重放原逐点欧拉演化与门控更新，保留全部原生观测，未修改连续单元或输入清单。前向中位数由 17.82 秒降至 0.94 秒，约提速 18.94 倍；三次重复的具体值见 [summary](cogpilot_summary.json)。此对照不包含完整优化器更新和验证成本，不据此估算整轮完成时间。

同参数完整窗口输出逐值一致，已连接参数的梯度最大绝对差为 `6.33e-7`；未来值扰动的历史最大差为零，未来缺失模式改变为 `4.77e-7`，均通过 `1e-6` 输出阈值。峰值分配显存由 832,330,240 字节降为 671,071,744 字节。恢复测试另覆盖两次梯度累积、不同长度的多个前向和冻结编码器，模型与优化器重放逐值一致。

最初分块方案输出一致但梯度错误，失败被保留。原因是图的静态梯度缓冲区被下一次重放覆盖；当前实现按块重算，返回前复制全部输入与参数梯度，不依赖共享的梯度存储。单步编译仅约 1.61 倍，整段脚本化未改善耗时；这些探查记录与失败数值均保留，未成为正式候选。PyTorch 图捕获发出累积节点流不一致提示，实际捕获成功，数值、恢复与训练检查分别验收。

加速由检查点配置显式记录，默认保持关闭；v4 原生训练可在本项正确性通过后启用。仅支持 FP32（32 位浮点数）、单步欧拉方案，短于 32 点或 CPU（中央处理器）的调用沿用原路径。分块补齐只添加零时间增量与无效掩码，参考查询仍读取观测更新形成的轨迹锚点。确认观测与任务成绩未打开。

复现入口：`scripts/evaluation/application_tasks/run_thesis_v4.py native-profile --domain cogpilot --output-root artifacts/application_evaluation/2026-09-06_v4-native-recurrence`。原型探查源码与剖析输出位于 `artifacts/application_evaluation/2026-09-06_v4-native-profile/`；正式实现及回归检查进入 `src/chronaris/models/alignment/cuda_recurrence.py` 与对应测试。

仿真原生短流的补充对照含生理 61 点、航电 301 点。保持 512 点执行块并对尾部使用零时间增量与无效掩码后，三次前后向中位数约提速 3.48 倍，输出逐值一致、参数梯度最大差为 `2.98e-8`。据此将启用下限从 512 点降至 32 点，补齐短流与长流的 CUDA 恢复测试；原生点数不变。
