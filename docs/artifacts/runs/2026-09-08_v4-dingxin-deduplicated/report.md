# 鼎新保留单记录与时间块评价

鼎新航电重复现已通过保留一份记录处理，原始五个快照文件及两架次失败证据完整保留。用户于 2026-09-08 判断可能为导出问题并授权保留其中一段或采用更合理方式；本次按架次标识固定字典序保留 `20251002_单01_ACT-8_翼云_J16_12#01` 及其两个生理视图，不按模型成绩或生理字段多少选择记录。

下表给出实际可用清单。所有隔离均覆盖完整 30 秒输入和其后 5 秒目标，两个视图同步分组，每个航电时段的视图权重总和为 1。

| 角色 | 航电时段 | 生理视图上下文 |
| --- | ---: | ---: |
| 编码器训练 | 12 | 24 |
| 内部验证 | 3 | 6 |
| 固定留出 | 3 | 6 |
| 编码器边界隔离 | 12 | 24 |
| 编码器冻结后的消费者训练 | 21 | 42 |

编码器训练支撑区间为相对 0–90 秒，验证为 90–135 秒，固定留出为 135–180 秒，均采用左闭右开区间。最终消费者可以使用留出之前的完整允许训练池，目标尺度、类别阈值与字段选择重新拟合该 21 时段，仍与留出支撑不相交。固定留出的三个窗口彼此有重叠，只作描述性展示，不能当作三个独立架次。

初次 6/6/6 方案因训练区间仅支持两项有变化的机动统计、低于原定四项而被拒绝；失败不放宽阈值。调整为优先保留 12 个训练时段、验证与留出各 3 个后，训练覆盖支持五项机动统计与 11 个生理字段；最终消费者同样保留 11 字段。调整依据为训练目标可构造性，没有产生或比较模型成绩。

[实际数据审计](data_audit.json)记录保留/排除记录、所有样本角色及内容交集；原生内容审计确认 30 个航电时段均唯一、训练与留出无重复内容。原始文件逐个 SHA-256 校验通过。新评价只使用一个记录和三个种子，替代原两折留一架次评价；不报告跨架次泛化或由窗口、种子构造的显著性区间。

[数据库前缀抽查](live_source_check.json)分别读取两组记录首秒全部航电测量，每组 163 点，与本地快照逐值一致。这支持当前快照忠实保留了已存在于数据库副本中的该段数据；仅凭这次抽查，尚不能判断重复最初发生于采集、导入还是更早的导出环节。本次没有修改数据库。

复现数据核验：使用仓库 `chronaris` 环境执行 `scripts/evaluation/application_tasks/run_thesis_v4.py dingxin-retained-data --domain dingxin`。CPU 确定性测试检查视图同步、完整时间支撑、目标参数不受留出扰动及拒绝丢弃真正不同的航电记录。正式模型成绩仍等待全局配置冻结。
