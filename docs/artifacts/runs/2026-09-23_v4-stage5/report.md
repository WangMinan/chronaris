# 阶段 5 首批固定配置复核

**2026-09-23：首批固定配置复核已启动，四个历史结果核验继承，首个新增单元正在运行。** 后台总控为 `1540752`，当前新单元为 CLARE 生理负荷数据、首折、种子 29 的 Chronaris 连续融合模型。32 个新增单元会按固定矩阵继续；13:04 核验时预训练已到 30/300 次，磁盘检查点保存到第 25 次，失败列表为空。当前尚无新增完整研究单元，也没有本批新的方法优劣结论。

## 固定范围与实际执行

本批按[执行协议](../../../requirements/thesis-stage5-execution-20260923.md)检验原参考的跨划分、跨种子稳定性。下表展示固定范围，支撑复核既有收益和不足是否稳定；不追加模型搜索，不以模型胜出作为完成条件。

| 数据与比较方法 | 固定设置 | 单元数 |
| --- | --- | ---: |
| CLARE 生理负荷数据：Chronaris 与 ContiFormer 连续时间变换器 | 三折、种子 17/29/43 | 18 |
| CogPilot 虚拟飞行数据：Chronaris 与第二输入流单流 | 三折、种子 17/29/43 | 18 |
| 合计 | 自监督与任务引导分别导出、分别拟合同类型下游 | 36 个单元、72 条表示路线 |

四个首折种子 17 的结果继承自阶段 4，保留原源码归属及历史成本，不记作新训练。[机器计划](stage5_plan.json)绑定 18 份数据合同、各单元预算和历史来源；1075 个原来源文件全部核验通过。[历史配置检查](historical_configuration_audit.json)核验八个检查点，确认原结构、学习率、损失选模和联合微调公共项权重 0.2。阶段 4.5 各轮失败候选不采用，原结论保留。

**新增训练固定为 17,600 次优化器更新。** 其中预训练 9,600 次、任务头预热 1,600 次、联合微调 6,400 次。工程资格短跑单独记录；历史四个单元不新增更新。预算中的基础配方与执行资格分开记录，Chronaris 按通过的资格启用显卡运算图，实际配置保存在各单元的 `recipe.json`。

## 已完成验证

**冻结源码的完整显卡回归通过：705 项通过、22 项跳过，约 400.56 秒。** [验证汇总](validation.json)和[原始收据](validation_receipt.json)绑定源码、测试集合及日志。十二份被忽略的测试数据逐一核验后复制至运行副本，见[复制记录](fixture_copy.json)。此前针对性测试为 15 项通过、七项未选入，随后新增的成功验证尝试引用检查已包含在完整回归中。

**六个开发划分均有有效分类和回归标签，真实新折新种子入口通过。** [数据预检](data_preflight.json)记录各角色数量；[真实短入口](real_entry_checks.json)覆盖第二折、种子 29 的 CLARE/Chronaris 与 CogPilot/第二输入流单流，各完成两次预训练和四次任务引导更新，并完成导出和共同下游。这些工程样本来自 `9db52151`；后续只修正编排引用成功验证尝试的方式，训练实现未改。完整回归和下面的执行资格均来自最终冻结源码。

下表汇总两域的独立执行资格，支撑本批启用图执行与更新边界恢复。普通执行与图执行使用既定数值容限；独立恢复逐值一致，固定分类消费者预测变化为零。

| 数据 | 执行轨迹对照 | 独立恢复状态类别 | 固定下游检查 | 结论 |
| --- | ---: | ---: | ---: | --- |
| CLARE | 54 | 6 | 10 | 全部通过 |
| CogPilot | 90 | 6 | 10 | 全部通过 |

完整数据见[执行资格汇总](execution_checks.json)、[CLARE 检查](clare_execution_check.json)和[CogPilot 检查](cogpilot_execution_check.json)。资格短跑耗时不外推完整单元加速倍数；后续按同域同方法完整单元的计时更新预算。

## 后台与复现入口

**源码与产物均使用独立目录，后台已进入新增研究训练。** 冻结提交为 `dd16e6885c26b03d2e54bbe5052132564e3ebdd4`；[启动记录](launch.json)保存命令和来源，[启动核验快照](launch_verification.json)保存阶段、进程及实际更新证据。

- 冻结副本：`/home/wangminan/projects/chronaris-runtime-stage5-20260923`
- 运行目录：`/mnt/e/chronaris-v4-results/2026-09-23-stage5`
- 统一入口：`scripts/evaluation/application_tasks/run_v4_pipeline.py --until stage5 --root <运行目录>`，须从上述冻结副本执行。
- `pipeline_state.json` 记录阶段、子进程和失败；`progress_summary.json` 按模型单元区分继承与新增完成量；各单元保存实际阶段耗时和检查点。

完成收据、来源或源码变化会拒绝复用，未知失败保留现场并停止；显卡资源等待可自动重试。不能从修改后的主工作区静默续跑该冻结目录。

## 阶段 5 后续工作

**本批完成后收口固定参考复核，再接续完整方法的正式实验。** 正式方法覆盖、279 个原生模型单元与 477 条实际表示路线见执行协议；近期模型没有因本批只比较强参照而被排除。完整正式预算与接口、模型文件冻结、主表、核心消融、机制、压力和案例仍需继续完成，独立确认尚未打开。

统一入口的 `stage5_completed` 终态仅表示本批完成；报告同时记录 `formal_evaluation_ready=false`，不代表阶段 5 的全部正式工作完成。当前研究目标是完整、公平、可复现地确认方法表现，所有有利和不利结果均保留。
