# v4 因果归一化与有效掩码修复

修复后的历史表示隔离通过原 1e-6 阈值。三个已训练种子 17、29、43 的权重显式加载到当前实现，仅用于诊断；不重写权重，不形成 v4 应用成绩。归一化改为每个查询可见历史内的统一尺度，并修复没有历史观测时放行未来点、分支偏置和语义池化掩码。

下表使用四个原训练内验证窗口和 15 秒截点，展示历史表示最大绝对变化；未来数值扰动沿用原审计的有效观测加 100000。

| 设备与种子 | 重复原输入 | 未来数值扰动 | 删除未来观测 |
| --- | ---: | ---: | ---: |
| CUDA 17 | 0 | 0 | 4.1723e-7 |
| CUDA 29 | 0 | 0 | 4.7684e-7 |
| CUDA 43 | 0 | 0 | 4.7684e-7 |
| CPU 17 | 0 | 0 | 0 |

四次输入检查点前后哈希一致。完整样本、来源代码指纹及结果见 [checkpoint_audit.json](checkpoint_audit.json)。执行函数为 `chronaris.evaluation.application_tasks.v4_correctness.audit_checkpoint_causality`，显式标记当前实现上的权重迁移诊断；不代表旧 v3 模型本身通过了原门。

新增测试同时检查语义填充不变性、各缺失分支严格为零、无历史注意力、事件分数梯度和混合时长增强；该批与相关原有测试合计 36 项通过、2 项环境跳过。完整测试在 v4 实施复核中汇总。
