# v4 修复参考的真实数据工程闭环

四个数据域均完成了自监督与任务引导两条路线的真实 CUDA（统一计算设备架构）训练、冻结表示和重新拟合固定消费者。历史输出检查全部通过，最终完整测试为 **524 项通过、8 项跳过**，用时 210.95 秒。跳过项为两个旧 NASA/UAB 实时数据检查与六个未启用运行时的占位检查，不包含本轮 v4 核心检查。来源与日志摘要见 [验证记录](validation_summary.json)。

下表核对本阶段实际预算与训练器耗时。每次自监督冒烟为十个完整更新；任务引导为 50 次任务头预热加十次联合更新。两条路线使用同一初始化条件，消费者从头训练。

| 数据与任务 | 自监督耗时 | 任务引导耗时 | 最大历史扰动 |
| --- | ---: | ---: | ---: |
| 新仿真三任务 | 77.82 秒 | 253.77 秒 | 4.77e-7 |
| 鼎新未来机动回归单任务 | 79.29 秒 | 278.43 秒 | 4.77e-7 |
| 鼎新三任务 | 复用上述初始化 | 271.75 秒 | 5.07e-7 |
| CLARE 认知负荷分类与回归 | 120.69 秒 | 304.46 秒 | 5.96e-7 |
| CogPilot 难度与离线事件条件响应 | 537.06 秒 | 981.60 秒 | 0 |

新仿真和公开冒烟使用固定哈希选出的 32 个训练与 8 个开发验证窗口；鼎新使用第一外层折内部的 18 个训练与 6 个验证视图。完整开发清单用于后续学习曲线，确认观测和确认模型得分未打开。仿真序列任务头已接入，完整序列消费者与持续时间解码在诊断阶段运行；此处固定消费者闭环覆盖标量任务。

短预算的消费者结果有升有降，不参与候选选型。鼎新单任务的任务引导均方根误差由 2.8818 升至 4.3853；CLARE 两路线分类宏平均 F1 均为 0.3846，回归误差由 4.8584 降至 4.4093；CogPilot 难度宏平均 F1 均为 0.5，事件响应误差由 0.3491 升至 0.7400。公开工程验证窗口很少，尤其 CogPilot 难度仅两个验证窗口，后续依据完整受试者分组与三种子结果作研究判断。

本组实际新增优化器更新合计 **350 次**，其中含一次失败尝试的十次自监督更新。CLARE 首次尝试因完整开发标签未裁剪到工程子集而在微调前停止；隔离约束正确拦截，裁剪修复后在新目录重跑。失败及原检查点保留，鼎新初始化复用不重复计数。具体阶段和文件见 [计算量台账](compute_ledger.json)。其他早期工程运行与测试开销各保留原记录，不并入这 350 次。

较早的鼎新与仿真运行使用提交 `988da854`，公开重跑使用 `9121c98e`。随后修复了大规模精确分位数计算，并核对三个已成功的 CLARE 完整开发折统计逐值不变。九个完整开发归一化划分已全部拟合成功，见[开发数据记录](../2026-09-06_v4-development-data/report.md)。

性能改动保持原连续单元和原生采样点。长序列图重放与短序列补齐均通过输出和梯度对照；任务头预热仅缓存完全相同实际批次的冻结编码结果，联合适配前清空，中央处理器（CPU）与 CUDA 的缓存对照及中断恢复状态逐值一致。鼎新上下文额外缓存的 CPU 探查仅约提速 1.22 倍，保留原实现。

重型检查点和更新日志位于 `artifacts/application_evaluation/2026-09-06_v4-real-domain-smoke/` 与 `2026-09-06_v4-real-domain-smoke-repair/`。复现入口为 `scripts/evaluation/application_tasks/run_thesis_v4.py smoke --domain <数据域>`；单任务增加 `--task-mode single`。本阶段完成修复参考与共享接口的工程验收，下一阶段开始完整训练诊断。
