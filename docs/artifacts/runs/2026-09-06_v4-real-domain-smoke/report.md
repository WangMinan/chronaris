# v4 真实数据双路线工程闭环

鼎新未来机动回归单任务已完成实际 CUDA（统一计算设备架构）训练、冻结表示与从头拟合消费者，两条路线训练后的历史最大扰动均为 `4.77e-7`。来源为提交 `988da854`，运行使用指定 RTX 4090 与 chronaris 环境；完整自监督与任务引导检查点分别保留。

下表核对首个单任务实际更新与耗时，只用于工程闭环和计算量诊断。

| 路线 | 完整更新 | 训练器记录耗时 | 内部验证消费者均方根误差 |
| --- | ---: | ---: | ---: |
| 自监督表示 | 10 次预训练 | 79.29 秒 | 2.8818 |
| 任务引导表示 | 50 次头预热＋10 次联合更新 | 278.43 秒 | 4.3853 |

任务引导在此短预算子集上的误差更高，原值保留，不参与候选选型。两条路线均在同一内部训练部分拟合标准化与目标参数，输入保留原生采样点，任务消费者固定使用岭回归。这里使用 18 个训练与 6 个验证视图；没有读取另一架次来生成模型得分。

鼎新同域三任务也已完成双路线工程闭环，当前串行推进新仿真、CLARE 认知负荷数据和 CogPilot 虚拟飞行数据。鼎新三任务复用该单任务的自监督初始化，参数、输入、预处理和目标一致时不重复预训练。公开冒烟采用固定哈希抽取的 32 个训练与 8 个验证原生窗口，属于开发组内的工程子集，完整开发清单用于后续学习曲线。

检查点和逐次更新日志位于 `artifacts/application_evaluation/2026-09-06_v4-real-domain-smoke/`；本目录保存紧凑摘要。恢复与来源检查继续生效，正式确认和候选结果尚未产生。

数据等待的独立 CPU 对照显示，缓存鼎新已构造上下文仅使四样本准备约提速 1.22 倍，全部批字段逐值一致。该路径暂保留原实现，探查数字见 `dingxin_context_cache_probe.json`。

CLARE 首次尝试完成了十次自监督更新，随后标签隔离检查发现完整开发标签未裁剪到 40 个工程窗口，微调尚未开始即停止。失败信息与原检查点保留；裁剪修复通过针对性检查，隔离约束不放宽。新运行目录为 `2026-09-06_v4-real-domain-smoke-repair`，不把已耗更新计为成功的双路线闭环。

随后针对性检查包括标签角色裁剪、最终心跳计数与图执行恢复，12 项通过。预热时按完全相同的实际批次缓存冻结编码结果，CPU 与 CUDA 对照的最终权重、优化器、随机状态和更新记录逐值一致；相关三项检查通过。缓存仅在任务头预热期间有效，联合适配前清空。
