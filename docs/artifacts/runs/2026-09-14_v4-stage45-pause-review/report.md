# 阶段 4.5 暂停复核与恢复

2026-09-14。按用户要求立即采用最近落盘检查点停止原队列，完成编码、真实入口验证后在独立目录恢复。原 72 小时硬上限已取消，固定开发矩阵完成后停止，阶段 5 与正式确认仍关闭。

## 状态保持与执行收益

第 75 次优化器更新的全部训练状态和采样位置已保留。原总控 445416、子进程 453545 已停止；暂停前内存约到第 90 次，未落盘的 15 次更新不复用。原运行的 1431 个文件建立哈希清单，保留停止终态和全部不利结果。恢复核验数据、任务、角色的 7 份合同，仅审查执行和报告代码变化；模型、损失、优化器计算源码保持一致。

下表对同一状态、同一数据顺序继续两次更新，支撑当前预训练状态的执行迁移；计时为前向、反向和优化器核心计算，不代表整个阶段的加速比。

| 执行方式 | 第 76、77 次更新计算耗时 | 一致性 |
| --- | ---: | --- |
| 普通执行 | 184.54 秒 | 参考 |
| 显卡运算图执行 | 20.21 秒 | 原数值容差通过，约 9.13 倍提速 |
| 独立进程恢复图执行 | 单独核验第 77 次 | 输出、状态、梯度、随机数及采样位置逐值一致 |

序列输出最大差 5.96×10⁻⁷，编码器参数最大差 2.98×10⁻⁸；显式时移头也纳入状态、梯度和优化器顺序检查。CLARE 生理数据上的冷启动资格失败仍保留，不据此改为通过。迁移只覆盖当前论文机制参考配置的预训练；进入任务引导微调前，派生权重完全一致、明确记录来源的普通执行初始化检查点。

单个普通执行微批次记录约 123 万次 `cudaLaunchKernel` 内核启动和约 6.3 万次扩展内核启动。大量小矩阵与逐元素操作产生调度成本，解释了显存占用低但设备繁忙的现象。该剖析包含显著工具开销，其时长未用于加速比。Nsight 在工作负载启动前连接超时，日志保留，后由 PyTorch 算子剖析完成取证；原始 2.5 GB 跟踪文件留在运行盘。

## 编码与验证

恢复流程复用 32 个完成的分支探针与执行资格步骤，重新核验来源和计划；旧 34 个完成步骤的证据全部保留。新队列为每次工作进程退出和中断保存墙钟成本，并在最终报告保留初筛结果、复核结果与恢复来源。旧成本只写检查点可证下界，不把丢失的 15 次更新当作零成本。

真实共同入口的短程显卡测试完成两次预训练、两次任务头预热、两次联合适配及下游导出，验证图预训练到普通微调的初始化权重一致；4 项检查通过。保留已有图捕获流同步警告，未隐藏或放宽数值判据。

复核原始材料位于 `/mnt/e/chronaris-v4-results/2026-09-14-stage45-pause-review`。紧凑证据见本目录 JSON 与测试记录，大型检查点和算子跟踪不入仓。

## 本次停止点

完整来源与实际恢复入口核验已于 17:54 完成：7 份合同和旧来源核验通过，真实共同入口从第 75 次继续第 76、77 次更新，编码器、公共任务头、显式时移头与图执行对照逐值一致；随后按诊断上限正常退出。见[结果](entry_validation.json)、[日志](entry_validation.log)和[恢复核验](entry-recovery.json)。

用户随后要求本轮核验后暂停，待手动唤醒再做完整回归并继续阶段 4.5。因此本轮完整显卡回归尚未运行、长期队列尚未启动、修改尚未提交。第 50 次能力快照脚本已准备，尚未实跑。下一步见[交接记录](HANDOFF.md)。


## 2026-09-14 17:58 完整回归启动

用户手动唤醒后已授权启动完整 CUDA 回归。总控 PID 635589，独立会话运行；输出目录 `/mnt/e/chronaris-v4-results/2026-09-14-stage45-recovery/cuda_validation/attempt_1`，测试输出为 `pytest.log`，结束时生成 `pytest.xml` 和 `validation_receipt.json`。只执行完整测试套件，完成后自行退出，不接续长期实验。源码与测试指纹已写入 `launch.json`；运行中不修改源码和测试。下次唤醒先检查凭证状态、退出结果和源码指纹，再决定阶段 4.5 后续执行。


## 完整回归验收与长期恢复授权

完整显卡回归 691 项通过、22 项跳过、0 失败，耗时 305.28 秒；凭证与当前源码、测试集、日志散列一致。用户已授权长期恢复，采用独立冻结源码和新运行根，从第 75 次状态继续固定阶段 4.5 矩阵，72 小时硬上限取消，阶段 5 关闭。第 50 次独立分支快照诊断不作为启动依赖；既有队列将按协议导出保留快照的共同下游结果。
