# 阶段 4.5 全流程加速与恢复

**2026-09-21 状态更新：本恢复队列已正常完成，18 个首轮单元全部结束，五种候选零入围。** 后续复核按规则跳过，阶段 5 未启动；完整结果见[收口报告](../2026-09-21_v4-stage45-closeout/report.md)。以下保留 9 月 19 日启动时的验收与现场记录。

2026-09-19。执行修订已经完成实现、全流程验收和来源核验，已在新目录启动恢复队列。冻结代码为 `4a09cc30`，实验范围覆盖整个阶段 4.5；原九个完整首轮单元及 CogPilot 虚拟飞行数据第 75 次训练状态保留。当前完成的是运行能力验收，剩余实验与最终候选判断仍由原固定矩阵产生。

## 加速范围与执行标准

显卡运算图重放（CUDA Graph）覆盖预训练、完整验证、任务头预热、联合微调、表示导出、快照诊断及后续开发复核。每个 Chronaris 微调单元只在第一次联合更新保留普通递推，后续至少 199 次联合更新使用图执行；对照模型沿用已有高效实现，已经完成的九个单元直接继承。中央处理器（CPU）和图形处理器（GPU）的此前实测及成本占比见[负载复核](../../../review/stage/thesis-v4/stage45-runtime-assessment-20260918/README.md)。

[执行容差修订](../../../requirements/thesis-stage45-execution-v2-20260918.md)将表示整体误差、局部误差、训练状态与固定下游预测分别约束。表示相对均方根误差上限为 0.0001，参数、梯度和优化器的绝对与相对容差为 0.00001、0.0001；同执行方式的独立恢复必须逐值一致。研究效果门、选模规则、候选、标签、数据角色、微批量 4、有效批量 16、单精度及更新预算保持原合同。

首次联合更新采用普通递推，解决了首次 Adam 优化器更新对近零梯度的放大。原额外测试中，一个梯度约为 0.0000000273 与 0.00000000212，更新后单个参数差 0.0000557，超出新合同；这份失败原样保留。执行调整后，同一生产批量测试的 38 份前向和更新记录逐值一致；第二次联合更新切回图执行，33 份记录的最大绝对差为 0.000000477，按既定判据通过，未继续放宽参数容差。

## 验收结果

下表区分完整自动化回归、不同配方资格、真实状态恢复和局部计时。局部加速倍数用于解释主要耗时改善，不作为整阶段固定倍数或剩余完成日期。

| 检查 | 结果及含义 |
| --- | --- |
| 完整 CUDA 自动化回归 | 697 通过、22 跳过、0 失败，324.06 秒；跳过项与警告保留于原始日志 |
| 三域、两类配方全流程资格 | 六组全部通过，覆盖预训练、微调、验证、导出、固定下游和独立恢复；固定分类决定相同、回归预测满足合同 |
| 原第 75 次连续六次预训练 | 第 76–81 次通过；核心计算普通执行 2914.81 秒、图执行 223.94 秒，约 13.0 倍 |
| 原第 50 次完整验证 | 1065 个验证窗口，342.47 秒；全部验证损失与机制指标对历史普通执行逐值一致 |
| 生产批量首次联合微调 | 含头预热、验证和导出，普通执行 955.13 秒、修订执行 767.56 秒；38 份记录逐值一致 |
| 生产批量第二次联合微调 | 单次联合更新、验证和导出，普通执行 753.80 秒、图执行 62.04 秒，约 12.2 倍；独立恢复逐值一致 |
| 当前源码的真实恢复入口 | 从第 75 次继续至第 77 次，模型、任务头、时移头、优化器和随机状态对图执行参照逐值一致，采样相同 |
| 验证前中断 | 预训练和微调先保存待验证状态；独立进程补做验证，不重复训练更新，最终状态逐值一致 |

最初在冻结副本运行完整回归时，缺少被 Git 忽略的历史特征数据文件；补齐 42 份相关文件并核对散列后，完整回归重新通过。之前的失败日志保留。自动启动曾错误地将主动停止后更新的进度文件与停止前备份作不可变比较；检查点与完整结果均未变化，改为按各自应有的冻结时点核验后继续启动，原失败记录保留。

## 来源与运行入口

新生产目录为 `/mnt/e/chronaris-v4-results/2026-09-18-stage45-accelerated`；冻结运行副本为 `/home/wangminan/projects/chronaris-runtime-stage45-execution-v2-stable-20260918`。恢复总控于 2026-09-19 20:00 启动，进程号 `1111625`。截至 2026-09-19 20:15:04，计划和全部数据合同核验已通过，九个完整首轮结果已继承，当前正在登记历史诊断，阶段索引为 `stage45_perf__clare__thesis_reference__check`，失败列表为空。新目录第 75 次状态与来源的规范训练状态散列一致，图执行已启用；下一步自动进入当前 CogPilot 单元续训，此次现场核验尚未观察到新的生产训练更新。六份后续复用检查点的状态迁移也全部通过。

第 75 次到第 100 次之间，进度文件反映已完成更新，周期检查点仍按每 25 次保存；新实现会在第 100 次完整验证前保存待验证状态。查看后续进度应使用上述新生产目录，原目录用于保留原始证据。

资格和验证根为 `/mnt/e/chronaris-v4-results/2026-09-18-stage45-execution-v2-stable`，主要机器凭证如下：

- `recovery/recovery_evidence_v2_complete.json`：冻结源码、旧新来源、原文件散列、六组资格与新增生产批量检查。
- `cuda_validation_complete/validation_receipt.json`：完整测试源码、测试集合和原生日志绑定。
- `qualification/performance/*/*/check.json`：三域两配方的执行、恢复与固定下游判断。
- `primary_finetuning/comparison.json`、`joint_continuation/comparison.json`：首次与后续联合更新及待验证状态独立恢复。
- `full_validation_comparison.json`、`real_resume_comparison.json`：完整验证及实际训练入口恢复。
- 新生产目录的 `launch.json`、`launch_verification.json`、`pipeline_state.json`、`recovery.json`：启动命令、现场进度及第 75 次迁移来源。

历史失败保留在 `/mnt/e/chronaris-v4-results/2026-09-18-stage45-execution-v2/primary_finetuning/comparison.json` 及原暂停测试目录中。原生产目录 `/mnt/e/chronaris-v4-results/2026-09-14-stage45-recovery` 保持停止；第 76–100 次未保存的 25 次更新按用户授权回退。新保存顺序避免之后在完整验证期间失去刚完成的训练更新。

后续先完成剩余九个首轮单元，再按原门决定开发复核与鼎新组件检查。现有 CLARE 生理负荷结果支持至多一个候选仍有入选可能；其余冻结单元照常完成，保留不利结果。整个阶段完成后，再用实际总墙钟决定正式矩阵预算；本次入口只运行到阶段 4.5，阶段 5 继续关闭。
