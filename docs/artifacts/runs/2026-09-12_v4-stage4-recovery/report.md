# 阶段 4 修复、成果复用与后台恢复

当前修复解决了共享入口对非 Chronaris 基线的参数传递错误。原总控 81269 于 2026 年 9 月 12 日 09:09:57 退出；此前 CLARE 生理数据上的 Chronaris 完成自监督 300 次更新，以及任务头预热 50 次和联合训练 200 次更新。生理单流基线在零更新时被参数校验拒绝。

这份已完成单元为阶段 4 提供了首份完整成本实测：共 52,192.68 秒，约 14.50 小时。自监督训练耗时 22,902.34 秒，任务引导训练耗时 28,389.33 秒，两次表示导出分别为 439.45 秒和 422.18 秒。它说明当前主要成本在训练；各基线及近期方法的成本仍须逐项实测。

下表保存首个单元的开发结果，分类指标为宏平均 F1 分数，回归指标为均方根误差（RMSE）。各值按验证受试者汇总，仅来自首个开发折和种子 17，当前尚不足以比较 Chronaris 与单流方法的优势。

| 表示训练方式 | 下游算法 | 认知负荷分类 F1 | 负荷评分回归 RMSE |
| --- | --- | ---: | ---: |
| 自监督 | 线性算法 | 0.451930 | 3.348443 |
| 任务引导 | 线性算法 | 0.445869 | 3.719257 |
| 自监督 | MiniROCKET 时序卷积特征方法 | 0.513477 | 2.920917 |
| 任务引导 | MiniROCKET 时序卷积特征方法 | 0.509498 | 2.918395 |

恢复方案保持原矩阵与预算。共享训练入口沿用其他原生入口已经使用的按方法分派方式：Chronaris 使用自身融合配置，其他方法使用训练器默认配置。新增真实基线入口回归覆盖生理单流、上下文构造第二输入流、跨模态 Transformer（MulT）和连续时间 Transformer（ContiFormer），执行训练、序列导出及两条路线的独立下游拟合。

已有成果通过显式复核进入新运行目录。原源码快照、输入、检查点、表示、下游模型、结果与失败记录统一保存散列；仅放行本次参数分派和恢复编排差异。全部训练及验证表示归档重新加载，每条路线、每个角色固定重编码 4 个窗口，共 16 个窗口核验检查点与序列一致；全部 428 个验证窗口的下游预测、指标及参数选择重新核验。原始数据、标签含义、分组、历史截止点及预处理角色必须逐项一致。

旧目录 `/mnt/e/chronaris-v4-results/2026-09-11-stage4-comparison` 保留原样；新目录为 `/mnt/e/chronaris-v4-results/2026-09-12-stage4-comparison-recovery`。恢复入口继续使用 `run_v4_pipeline.py --until comparison`，增加 `--comparison-parent` 指向旧目录。新总控只在复用验收通过后承认旧单元完成，并继续失败基线及余下队列。成本汇总同时纳入父运行的成功和失败尝试，以及本轮新增复核成本。

完整 CUDA（统一计算设备架构）验证与实际恢复证据待本轮核验后回填。后台启动核验后按用户原有要求结束会话，等待后续唤醒；不把恢复启动视为阶段 4 完成。

源码与数据复核已通过：522 个旧源码及运行文件已绑定，原矩阵、预算和三个数据域的完整合同一致；差异仅为共享入口参数分派和恢复编排四个文件。详见 [复用清单](reuse_inventory.json)和 [新运行计划](plan.json)。四种可训练基线的独立实跑检查通过，见 [基线入口检查](baseline-entry.xml)。
