# 受控仿真跨方法有效查询审计修订

状态：修复完成，等待从新产物根恢复表示导出。

论文级受控仿真已在提交 `58db4458` 上完成 15/15 个预训练 checkpoint，9/9 验收通过；训练阶段未打开 G2、任务真值或下游指标。首次 clean 表示导出在跨方法对齐检查处 fail-fast，因为旧检查要求六种方法的有效查询 mask 逐点相同。

检查失败前已落盘的 seed 17 表示显示，train 角色六方法有效点数依次为 `36290/36480/36864/36480/36480/36480`，validation 为 `9074/9120/9216/9120/9120/9120`，held-out 为 `18138/18240/18432/18240/18240/18240`。差异来自生理单流、航电单流和朴素同步在首个有效观测前采用各自真实支持；样本顺序、96 个查询时刻和来源哈希保持一致。

评价协议 v3.2.1 仅修订这个审计假设：每个 `FusionStreamBatch` 继续独立校验 mask 与基于 mask 的池化；跨方法仍强制样本、查询时间和来源一致，并将每个方法名称和 mask 字节写入对齐 SHA-256。冻结 checkpoint 按文件 SHA-256 复核后复用，旧失败输出保留，新产物写入独立根。

聚焦测试 `22 passed`，三个实际角色的新对齐哈希均生成成功。本修订不改变模型、训练预算、数据、损失、消费者、随机种子或外层协议。
