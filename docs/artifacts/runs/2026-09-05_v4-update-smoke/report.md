# v4 完整更新、恢复与任务头实现证据

新共享训练器完成十次 CUDA（统一计算设备架构）优化器更新，实际批量 4、有效批量 32，对应 80 个前向批次和 320 次样本抽取。实测包含训练、验证与检查点的耗时为 79.66 秒，峰值张量显存约 125.6 MiB，数据读取等待共 0.174 秒。此冒烟复用历史状态空间仿真观测，改取 30 秒起始上下文，只验证工程合同，不承担候选选型。

训练中观测锚定权重始终为 1，前十次更新的机制权重均为零。数据游标、增强迭代、随机状态与阶段更新数进入完整更新边界检查点；默认每 25 更新保存，本冒烟每 5 更新保存。验证后再次检查历史信息隔离：未来值扰动差为零，未来缺失改动的最大差为 `4.77e-7`。

梯度累积中途退出后的重放已在 CPU（中央处理器）和 CUDA 上对照连续训练：权重、优化器、随机状态、训练审计行和数据游标相同，未完成的累积批次从最近完整检查点重做。批量、源码、样本来源及目标标签变化会拒绝静默恢复。实际批次中的负例数量仍由实际批量决定。

微调任务头支持可变类别数、标量与多字段回归、序列分类及任务有效掩码。各任务按有效样本求平均后等权汇总，目标标准化只拟合内部训练样本，导出使用真实有效点池化。已训练任务头不用于最终固定消费者的初始化。任务引导的头预热、联合适配更新日程和保留自监督目标仍待接入。

训练与任务头聚焦测试 21 项通过；鼎新拟合与更新边界另有 11 项通过。完整测试为 507 项通过、8 项跳过，用时 308.54 秒；第一交付点仍需完成公共模态目标与完整任务引导日程。具体来源、配置、训练清单与因果检查见 [summary.json](summary.json)。检查点位于 `artifacts/application_evaluation/2026-09-05_v4-update-smoke/chronaris/C/`。
