# 阶段 4.5-B 复盘与阶段 4.5-C 单因素尝试

2026-09-22。用户已授权继续尝试；本轮先修正诊断中的无标签解释，再按[新协议](../../../requirements/thesis-stage45c-development-20260922.md)检验联合微调是否需要公共自监督辅助项。真实短入口与完整显卡回归均已通过，生产队列于 9 月 22 日 19:34 正常完成；9 月 23 日已复核全部结果。

阶段 4.5-B 于 14:23 正常结束，总墙钟约 25.74 小时；十二次首轮训练全部完成，无运行失败，零入围，九个复核槽位按规则跳过。原检查点来源核验通过，阶段 5 未打开。这轮排除了“同时移除两条分支归一化即可整体改善”的直接采用方案，也表明新选模不是稳定升级；原结构、梯度目标与选模仍需分别研究。

本轮只改变联合微调公共自监督权重：0.2 改为 0。结构、预训练与选模不变，三个融合单元及三个强参照单元从相同预训练起点重新微调，六份预训练直接复用。预训练源码、数据角色、目标、归一化与父运行文件散列必须一致；新计算和旧结果分开保存。


## 完成结果与采用结论

**六次新增微调全部完成，关闭公共自监督项未达到采用门。** 总控 `1453273` 于 9 月 22 日 15:06 启动、19:34 正常结束，总墙钟约 4.47 小时，无失败或重试。每个单元完成 50 次任务头预热和 200 次联合更新，共 1,500 次新增更新；预训练原样复用。八份完成收据及 77 份来源文件散列再次核验通过，阶段 5 保持关闭。完整汇总、逐单元更新及原损失选模对照见[完成证据](completion.json)。

下表给出任务引导路线的配对主结果：分类为宏平均 F1（越高越好），回归为均方根误差 RMSE（越低越好）。CLARE 生理负荷数据对两个训练方向等权平均；CogPilot 虚拟飞行数据沿用固定训练与开发验证角色。自监督结果直接继承，数值不变。

| 数据与方法 | 分类：原权重 0.2 → 权重 0 | 回归：原权重 0.2 → 权重 0 |
| --- | ---: | ---: |
| CLARE 融合模型 | 0.400399 → 0.400399 | 2.304402 → 2.315876 |
| CogPilot 融合模型 | 0.234668 → 0.236565 | 0.310357 → 0.310388 |
| CLARE 强参照 | 0.318607 → 0.317999 | 1.609201 → 1.620372 |
| CogPilot 强参照 | 0.464957 → 0.461728 | 0.306514 → 0.306577 |

融合模型的 CogPilot 分类仅提高 0.001897，低于预定 0.02 改善门；CLARE 分类不变、回归误差增加约 0.50%，CogPilot 回归基本不变且略差。所有退化都在保护范围内，但达到有意义改善的域与路线组合为零，要求至少两个；强参照同样没有明显收益。本轮不采用权重为零的配置，也不自动启动下一轮或开放阶段 5。此结论说明该单因素尝试没有产生足够收益，不等同于排除所有后续研究方向。

复核顺序为本节、[完成证据](completion.json)、[固定协议](../../../requirements/thesis-stage45c-development-20260922.md)。执行入口为 `src/chronaris/evaluation/application_tasks/stage45c.py`，沿用原比较函数与训练实现；下文保留上轮复盘和本轮启动时的历史快照。

## 上轮完整结果

下表在共同线性下游下对比 CLARE 生理负荷数据与 CogPilot 虚拟飞行数据的全部首轮方案。CLARE 两个训练方向等权平均；分类列为宏平均 F1，回归列为均方根误差（RMSE），误差保留各数据域原生单位。自监督与任务引导分别比较。完整逐单元结果和父运行散列见[机器证据](evidence.json)。

| 数据域 | 方案 | 路线 | 分类宏平均 F1 | 回归 RMSE |
| --- | --- | --- | ---: | ---: |
| CLARE | 原结构参考 | 自监督 | 0.404309 | 2.442344 |
| CLARE | 原结构参考 | 任务引导 | 0.400399 | 2.304402 |
| CLARE | 机制组合 | 自监督 | 0.397986 | 2.746096 |
| CLARE | 机制组合 | 任务引导 | 0.382937 | 2.879458 |
| CLARE | 移除分支归一化 | 自监督 | 0.409579 | 3.036666 |
| CLARE | 移除分支归一化 | 任务引导 | 0.440112 | 2.824584 |
| CLARE | 强参照 | 自监督 | 0.319571 | 1.682803 |
| CLARE | 强参照 | 任务引导 | 0.318607 | 1.609201 |
| CogPilot | 原结构参考 | 自监督 | 0.234057 | 0.310286 |
| CogPilot | 原结构参考 | 任务引导 | 0.234668 | 0.310357 |
| CogPilot | 机制组合 | 自监督 | 0.223425 | 0.318327 |
| CogPilot | 机制组合 | 任务引导 | 0.212357 | 0.319126 |
| CogPilot | 移除分支归一化 | 自监督 | 0.264292 | 0.308411 |
| CogPilot | 移除分支归一化 | 任务引导 | 0.322818 | 0.309446 |
| CogPilot | 强参照 | 自监督 | 0.459081 | 0.306795 |
| CogPilot | 强参照 | 任务引导 | 0.464957 | 0.306514 |

机制组合没有达到有意义改善的域与路线组合；移除归一化虽改善 CogPilot 任务引导分类，但 CLARE 回归损失超出原保护范围，两种候选均不合格。强参照分别为 CLARE 连续时间变换器和 CogPilot 第二输入流单流。

## 有效标签梯度复盘

**新诊断支持尝试公共目标消融，同时显示影响存在受试者差异。** CLARE 生理负荷数据的四个批次都有分类与回归标签，任务间梯度余弦均为正（0.085—0.355）；公共目标与分类、回归均正负各半。CogPilot 虚拟飞行数据的四个分类有效批次有三个与公共项方向相反（范围 −0.299—0.073）；四个回归有效批次正负各半（−0.378—0.179）。本轮固定权重消融仍按原计划执行，不据此新增损失算法或更换结构。

CogPilot 两类标签落在不同窗口：分类批次没有回归标签，回归批次没有分类标签，因此同批次任务间夹角不可用。旧诊断的零回归梯度不能证明断开或冲突；已修正为无标签任务不可用，并记录每项有效数。本次十二个批次均只取训练受试者，没有更新优化器或触及外层。完整批次、来源检查点散列及夹角见[梯度证据](gradients.json)，抽样与调用入口见[诊断脚本](diagnose.py)。

**训练内分组选模仍只是待验证的固定主规则。** 上轮原结构的 CLARE 任务引导在新规则下平均 F1 为 0.400399、RMSE 为 2.304402；同轨迹原损失选模分别为 0.408973、2.059523。CogPilot 新规则分类稍好、回归相近。保留这一不利对照，本轮继续同时导出两种规则，避免同时改变损失与选模。

## 工程验证

**冻结源码 `c3b8f50d` 的真实 CLARE 入口已通过短验收。** 使用完整开发数据角色和合法父预训练，完成两次任务头预热、两次联合更新、新旧选模导出及共同下游，约 259.42 秒；新增预训练为零。该短配方仅检查工程链路，不作为研究成绩或生产耗时估计。机器结果在 `/mnt/e/chronaris-v4-results/2026-09-22-stage45c-real-check-frozen/screen/0.json`。

首次短跑在开发源码被补充核验逻辑后，由共同合同的源码指纹检查拒绝；原目录 `/mnt/e/chronaris-v4-results/2026-09-22-stage45c-real-check` 保留。随后在独立冻结副本和新目录完成验收，没有绕过来源检查。完整回归使用同一冻结副本；既有十二个被忽略测试数据文件按散列复制，记录在验证目录 `fixture_copy.json`。

新增显卡测试检查原样复用预训练、零公共权重、实际联合更新、父目录所有文件不变，以及数据合同变化必须拒绝。另用六份真实父结果验证配对汇总：候选和强参照的变化均为零、没有虚假入围。训练、检查点与恢复逻辑复用既有实现。

## 完整验收与后台交接

**完整 CUDA（统一计算设备架构）回归通过，702 项通过、22 项跳过。** 冻结代码为 `c3b8f50d9b24aa2235b9e3e6b8d3cf86f74c310a`，副本 `/home/wangminan/projects/chronaris-runtime-stage45c-20260922`。完整测试约 514 秒，源码与测试集合散列均绑定在 `/mnt/e/chronaris-v4-results/2026-09-22-stage45c-validation/cuda/validation_receipt.json`。

**新队列于 2026-09-22 15:06（北京时间）启动，总控为 `1453273`。** 运行目录 `/mnt/e/chronaris-v4-results/2026-09-22-stage45c`，来源计划已通过，首个 CLARE 原结构微调工作进程为 `1453302`。六次新增微调的总墙钟硬上限为 24 小时；此上限不是完成时间预测。原研究门、历史主结果与阶段 5 关闭状态保留。

生产 `pipeline_state.json` 记录阶段与失败；`launch.json`、`launch_verification.json` 记录启动命令与核验快照。启动核验后结束会话，实验在独立后台进程继续运行，等待用户手动唤醒，不建立持续通知或自动扩展实验。

启动核验时，首个 CLARE 单元的末次检查点已保存 50 次任务头预热更新，公共项权重为 0；总控与工作进程均存活，失败列表为空，父来源文件再次复核不变。该快照证明实际训练已开始，尚无新增生产单元完成，也尚无候选效果结论。
