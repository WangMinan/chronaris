# 完整开发比较的执行版本迁移与恢复

本轮从已保留的第 200 次更新恢复实验，并将经过真实更新与恢复对照的 CUDA Graph（显卡运算图重放）用于 CogPilot 虚拟飞行数据上的 Chronaris 连续融合模型。CLARE 生理数据上的九个完成模型单元、十五条表示路线继续复用，鼎新和其他方法沿原执行设置，正式确认继续关闭。

迁移在新目录进行，原目录与原检查点保持保留。迁移检查点只调整执行配置、对应的协议散列和来源记录，不改动模型、任务头、优化器、随机状态、采样位置及累计训练耗时；第 200 次以前约 24.55 小时计算继续保留。性能试验中的诊断更新不作为生产恢复起点，停止程序失误期间的六次未落盘更新将正常重算。

计算模块的源码须与保留版本一致；入口只允许已复核的执行开关改动。九个完成单元保持原逐路线结果、比较键及下游文件来源，新回执记录数据合同的对应关系和零额外训练更新。最终成本汇总合并历次成功、失败和主动终止的尝试记录，性能试验及迁移开销单独留据。

真实迁移已完成：800 个原文件核验通过，两个迁移检查点的训练状态散列与原第 200 次检查点一致。完整 CUDA（统一计算设备架构）回归为 683 项通过、10 项跳过，另两项固定模型资产测试全部通过。首次回归因冻结工作区缺少被 Git 忽略的历史测试附件失败；补齐十二个已有数据文件的链接后通过，初次失败及中止的校验记录保留。冻结代码为 `2b7db555`，总控 `376745`、子进程 `376801` 已启动，已确认从第 200 次恢复并写出第 225 次新检查点；21:09 现场累计更新为 230，设备为 CUDA，显卡利用率采样 99%，队列没有失败记录。验收入口见[复核记录](../../../review/stage/thesis-v4/stage4-execution-migration-20260913/README.md)。


## 迁移与启动证据

[迁移回执](migration_receipt.json)绑定原目录和性能试验，记录 800 个核验文件、九个继承单元、两份不可覆盖的迁移检查点及全部父级尝试成本。九个单元的原始逐路线结果不回写；数据与下游合同除源码和合同散列外逐项相同，新回执明确保留原比较键和来源。

[完整显卡校验](validation_receipt.json)与[固定资产补充校验](supplemental_validation.json)记录同一冻结实现的测试证据。[历史测试附件链接](runtime_fixture_links.json)说明冻结副本需要的既有数据来源，未新增数据。主回归中的两项固定资产测试跳过后，通过[补充测试](recent_assets_checks.xml)实际执行。

[启动凭证](launch.json)记录后台进程与命令。新运行目录为 `/mnt/e/chronaris-v4-results/2026-09-13-stage4-graph-recovery`，冻结工作区为 `/home/wangminan/projects/chronaris-runtime-stage4-graph-20260913`。模型计算逻辑未改变，仅在 CogPilot 的 Chronaris 双路线使用显卡运算图重放；其余方法与数据域保持原执行配置。正式确认继续关闭。

[真实训练核验](live_training_check.json)确认总控的父进程为系统进程，训练子进程属于新总控；保存点计数为 225、样本暴露为 3600，显卡运算图重放在训练配置与编码器配置中均开启。新检查点训练状态散列通过，累计训练耗时 88,947.50 秒继续包含原第 200 次以前的 88,363.41 秒。原第 200 次和最佳迁移检查点另外封存在新目录的 `migration/checkpoints` 中，不随训练检查点覆盖。启动核验结束后保留后台运行，不等待整个比较矩阵完成。

[启动后原成果复核](parent_preservation_after_launch.json)于 21:11 再次逐文件核验全部 800 个父级文件，内容散列全部未变；复核期间新训练继续运行。
