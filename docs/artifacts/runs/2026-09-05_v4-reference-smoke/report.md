# v4 观测锚定与物理标定工程冒烟

新观测锚定已实际接入 CUDA 训练总损失，完成十次更新后历史信息隔离仍通过。此次使用四个不同参数档案的 G1 训练窗口和四个训练内验证窗口，保留一个不参与训练的占位留出窗口；没有打开下游任务真值或计算应用成绩。

Chronaris 使用 32 维隐状态、安全滞后融合和可学习语义查询，关闭旧共享事件库配对目标。观测锚定权重为 1，标定后的物理残差权重为 0.05。五条关系为速度—纵向加速度、高度—垂向速度、三个角度—角速度；反归一化及残差尺度只拟合四个训练窗口，写入并从检查点恢复。

下表展示工程执行量及最后一次内部验证，支撑“损失和梯度确实接通”，不用于候选排名或机制效果论断。

| 项目 | 实测 |
| --- | ---: |
| 优化器实际更新 | 10 |
| 训练循环耗时（含验证及检查点保存） | 105.35 秒 |
| 最后验证观测锚定损失 | 0.259814 |
| 最后验证标定物理残差 | 0.570246 |
| 对应物理加权损失 | 0.028512 |
| 最后训练裁剪前梯度范数 | 0.923204 |
| 未来数值扰动的历史表示变化 | 0 |
| 删除未来观测的历史表示变化 | 4.7684e-7 |

完整来源指纹、标定参数、检查点路径及验证值见 [summary.json](summary.json)。重型权重位于被忽略的 `artifacts/application_evaluation/2026-09-05_v4-reference-smoke/`，不进入 Git。

该冒烟通过既有按轮入口实现每轮一次更新；v4 按更新控制的预算、梯度累积与 50–200 更新预热仍由下一批实现，因此不能将本次冒烟当作正式 v4 训练日程或耗时承诺。零物理输出的数学测试已经验证：它可以产生零物理残差，但无法同时通过同头观测锚定，且锚定梯度非零。
