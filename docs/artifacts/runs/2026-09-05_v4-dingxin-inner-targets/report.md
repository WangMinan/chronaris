# v4 鼎新内部训练目标拟合复核

实际快照的 90 个完整视图上下文、60 个独立航电上下文均通过新拟合接口。两个留一架次折分别按最后六个航电时间块构成验证部分，排除完整输入与未来目标区间的重叠后，各有 18 个训练、6 个验证、6 个隔离航电上下文；共享航电视图同步分组。

显式内部训练样本清单决定运动学评分尺度、类别三分位阈值、生理字段选择及字段尺度。内部拟合两折分别保留 12、11 个生理字段；编码器冻结后的外层训练架次重拟合也分别保留 12、11 个字段。这里只检查目标与数据合同，尚无鼎新新模型成绩。

回归测试改变内部验证部分的未来运动学与生理统计后，拟合参数完全不变。接口同时拒绝引入外层留出样本和拆散共享航电视图。具体清单与来源见 [summary.json](summary.json)；参数表保留在 `artifacts/application_evaluation/2026-09-05_v4-dingxin-inner-targets/`。
