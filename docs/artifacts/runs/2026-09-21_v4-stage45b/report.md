# 阶段 4.5-B 实现与实验交接

2026-09-21。本轮已按用户授权落实[表示保留与选模一致性协议](../../../requirements/thesis-stage45b-development-20260921.md)，完整 CUDA（统一计算设备架构）验证已通过，后台队列已于 12:39 启动。

新实现复用共同线性下游、既有训练与后台恢复入口，增加训练内分组的标签辅助检查点选择、同轨迹原选择对照，以及仅移除分支归一化的条件候选。CLARE 两个训练受试者采用固定双向留组，两个原验证受试者仍作开发验收；最多八个配置与域组合、十二次首轮训练，整个队列预算上限为 48 小时。

阶段 4.5 历史 18/18 完成、零入围的结论继续保留，阶段 5 关闭。冻结代码为 `0150c397`，完整回归 701 项通过、22 项跳过；[机器证据](evidence.json)记录验证与启动快照。

## 已完成的工程实跑

三组真实公开数据短入口已完成双路线训练、训练内选模、导出及共同下游。CLARE 去归一化为 72.88 秒、机制组合为 82.55 秒，CogPilot 第二输入流单流为 27.52 秒；每组为两次预训练、两次头预热、两次联合更新。这些是工程样本计时，不外推完整矩阵成本。结果保存在 `/mnt/e/chronaris-v4-results/2026-09-21-stage45b-real-check-v3/summary.json`，保留每组实际源码散列。

真实机制检查点的两条路线也已分别完成 16 组分支探针与两个固定批次的单项梯度测量，验证程序可实际读出归一化中间表示、同掩码结果、特征谱和梯度夹角；本次没有对旧检查点执行优化器更新。诊断入口验收在 `/mnt/e/chronaris-v4-results/2026-09-21-stage45b-diagnostic-check/summary.json`，仅证明诊断代码可运行，正式诊断结果由生产队列在旧完整模型上生成。

首次真实冒烟发现原抽样可能漏掉受试者的类别，第二次发现归一化器保存的样本顺序不同。已分别改为保留受试者类别支持的工程抽样、按样本集合核验训练边界；失败目录原样保留，第三个新目录完成实跑。正式训练角色不随这些工程修复改变。

独立源码副本的首次完整回归发现历史特征导出测试依赖被忽略的大文件。已从原目录复制 12 个文件、约 33 MB，逐一核对散列；记录为 `/mnt/e/chronaris-v4-results/2026-09-21-stage45b-validation/fixture_copy.json`。首次失败和后续重跑分别保存，不覆盖失败凭证。

## 最终验收与后台状态

**本轮代码与真实入口已通过验收，生产队列正在做来源和数据合同核验。** 冻结副本为 `/home/wangminan/projects/chronaris-runtime-stage45b-20260921`，代码 `0150c397717b727822b30b1db42c810bd0d41d69`；源码与测试集合均绑定在完整 CUDA 凭证中。新增测试覆盖分组隔离、标签污染、同分取较早检查点、普通与图执行数值，以及预训练和联合微调在独立进程中的中断恢复。

真实数据执行对照的 56 项检查通过，六次更新的状态对照最大绝对差约 `6.78e-7`；同一组已拟合线性与时序卷积特征消费者的分类预测均无变化，回归差异通过既定执行修订标准。两路线最佳与末次检查点的模型、优化器及随机状态共 12 项恢复比较逐值一致；普通与图执行的四份随机状态也逐值一致。完整原始证据位于 `/mnt/e/chronaris-v4-results/2026-09-21-stage45b-validation/`，复合启动凭证为 `validation_with_projection.json`，包含完整测试和执行检查的文件散列。原缺失测试数据导致的失败批次在定位后主动停止，保留 568 项通过和一项失败的现场；补齐后完整重跑为 701 通过、22 跳过。

**后台总控为 `1351224`，当前处于计划核验，尚未开始新增神经网络更新。** 2026-09-21 12:39:58（北京时间）检查时，工作进程 `1351230` 正在执行 `stage45b_plan`，总控和工作进程均存活，失败列表为空；显卡当时空闲，符合本步骤主要进行中央处理器（CPU）数据处理的状态。启动前独立预检已完成六份合同及 14 份历史检查点来源核验。

生产目录为 `/mnt/e/chronaris-v4-results/2026-09-21-stage45b`。`pipeline_state.json` 给出总控阶段与子进程，`logs/` 保存步骤输出，后续训练目录中的 `progress.json` 和检查点给出实际更新计数；`launch.json` 与 `launch_verification.json` 保存启动命令和本次快照。该状态是启动核验记录，不是训练已完成或候选通过的声明。

队列会自动先完成旧模型诊断，再执行固定首轮矩阵；去归一化候选按预定诊断触发，最多一名入围者继续固定种子复核。整个队列 48 小时硬上限，阶段 5 关闭。启动核验后结束本次会话，等待用户手动唤醒，不另设持续监控。
