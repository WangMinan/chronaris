# 运动学一致性目标收窄与训练内重跑

状态：**修复完成，等待 v3.2 全量训练内重跑**。

评价协议 v3.1 在冻结 commit `364c46ef84e6002233b4764aba84d40ac0d09088` 上完成 144/144 个训练内单元：仿真 12、鼎新 60、CogPilot 12、CLARE 60，外层结果始终关闭。仿真均衡五分类时移中，基础候选三随机种子均为 `0.2000`，显式时移候选为 `0.2000/0.2545/0.2364`；后两随机种子高于基础和随机水平。语义配对候选的正确—错误相似度差为 `0.2130/0.1664/0.1986`，R@1 为 `0.9091/1.0000/0.9091`。两项新机制门通过。

但 v3.1 的公共数据物理项出现不可接受的尺度：CogPilot 示例原始物理值约 `1.76e17`、加权值约 `1.76e16`；CLARE 示例原始值约 `1.71e16`、加权值约 `1.71e15`。公共自监督项本身保持有限，异常来自对高采样率脑电或生理重构计算二阶时间导数。该项不属于论文确认的航电运动学一致性口径，并通过梯度裁剪持续主导训练。因此 v3.1 全部结果标记 `superseded_due_to_physics_objective_scale_defect`，不进入候选排序或外层实验。

v3.2 只修改这个原因：物理训练目标限定为当前字段能够支持的平移、垂向和旋转航电运动学残差；生理平滑和血氧变化记录为超出运动学合同，不参与损失。真实 CogPilot 原生窗口 CUDA 前向确认垂向残差有效、计数 `268`、原始值 `8.17e-4`；平移与旋转因字段不足不可用，两项生理伪物理项均明确不可用。聚焦测试 `20 passed`，完整测试 `451 passed, 8 skipped`，静态检查和 `git diff --check` 通过。

恢复命令：

```bash
/home/wangminan/env/anaconda3/envs/chronaris/bin/python scripts/research/run_candidate_screen_sim_dingxin.py --stages simulation dingxin cogpilot clare --protocol-version v3.2 --resume
```

本修复不改变数据、分组、时间戳、候选、损失权重、数值积分、消费者或外层协议，并用尽预注册允许的训练内改进轮次。
