# 阶段 4.5 编码、验证与运行

2026-09-14。按用户要求在当前研究分支完成全部编码，再启动独立后台开发实验。方案见[模型与性能优化](../../../requirements/thesis-stage45-development-20260914.md)，发现与检查见[复核记录](../../../review/stage/thesis-v4/stage45-20260914/README.md)。

## 已完成工程检查

明确参考、既有注意力和保真候选、两种学习率、分支探针、固定快照下游、机制损失与相关梯度记录已接入现有共同入口。总控覆盖两域候选、两个新增开发设置和鼎新回归复核，最长 72 小时；正式确认继续关闭。

针对性检查 26 项通过、1 项显式真实入口跳过。独立 CLARE 生理数据显卡机制入口随后完成 1 项通过：两次预训练、两次任务头预热、两次联合适配，实际机制权重、有效样本与梯度检查通过。保留显卡运算图首次捕获的流同步警告。

CLARE 两个配置的普通执行、显卡运算图短入口及独立恢复已经实际完成；图执行资格均未通过，保留普通执行回退。CogPilot 虚拟飞行数据与鼎新的资格检查在后台队列逐域执行。完整显卡回归为 **688 项通过、21 项跳过、415 条警告**，耗时 322.79 秒；见[验证凭证](cuda-validation.json)、[测试明细](cuda-tests.xml)和[完整日志](cuda-tests.log)。当前不声称已有新模型收益。

真实 CLARE 短入口的最后一个联合微批次记录 196 个有效对齐位置、34,072 个观测锚定有效项、4 个显式时移样本；显式时移头梯度范数 0.3515、语义模块梯度范数约 8.85×10⁻⁶。运动学关系有效数为零，未写成物理关系收益。这些梯度来自联合目标，只证明相关参数参与了优化。


## 显卡运算图资格的实际结果

这组对照说明从头训练的图执行不能直接继承旧 CogPilot 第 200 次更新后的加速资格。两种配置的独立进程中断恢复均逐值一致，但普通执行与图执行间仍存在超限差异，因此两者都不获得当前数值合同下的图执行资格。

| CLARE 配置 | 最大序列差 | 更新状态最大差 | 独立恢复 | 图执行资格 |
| --- | ---: | ---: | --- | --- |
| 阶段 4 参考 | 8.047×10⁻⁷ | 3.293×10⁻⁶ | 逐值一致 | 未通过参数容差 |
| 声明的机制参考 | 8.235×10⁻⁴ | 5.022×10⁻⁶ | 逐值一致 | 未通过序列与状态容差 |

参考配置的差异定位到第二输入流连续递推参数：首步某个梯度由 4.4812×10⁻⁹ 变为 4.4126×10⁻⁹，Adam 优化器将其放大为约 3.286×10⁻⁶ 的参数差异。未更改优化器、积分方法、数值阈值或历史结果。机制参考的较大序列差也完整保留，未当作可忽略误差。

[资格汇总](qualification_summary.json)、[参考逐项对照](clare-stage4_reference-qualification.json)、[机制逐项对照](clare-thesis_reference-qualification.json)保存全部差异。原测试最初将“图执行资格必须成功”作为断言，产生两项失败，原[日志](qualification-original.log)及[明细](qualification-original.xml)保留；工程测试随后区分资格结果与记录、恢复正确性，并增加资格拒绝后确实走普通执行的检查。资格结论始终为未通过。

当前[数据计划](data-plan.json)固定 7 份合同、最多 18 个首轮单元，源码及每个单元的数据和目标均单独核验。预算上限可能先于完整候选矩阵耗尽，届时保留部分结果，等待人工判断。

## 交付与执行

全部代码和工程检查已收口，代码提交为 `34b0f0829197e93546ef143cdbb43dab1b4949d6`。独立副本位于 `/home/wangminan/projects/chronaris-runtime-stage45-20260914`，源码及测试指纹与完整显卡凭证一致。

后台总控 **445416** 已启动，父进程为系统进程 1；2026-09-14 13:09 的一次核验确认当前为来源与合同核验步骤 `stage45_plan`，子进程 **445422**，失败列表和错误标记均为空。旧参考检查点存在，显卡当时约 1% 利用率；尚未观察到新的训练更新，不能把启动写成优化完成。见[启动回执](launch.json)和[一次核验](launch_verification.json)。

结果根为 `/mnt/e/chronaris-v4-results/2026-09-14-stage45`。总控继续执行分支探针、逐域加速资格检查、候选与开发复核，最多 72 小时，不接续阶段 5。按用户要求在这次启动核验后结束会话，等待手动唤醒。
