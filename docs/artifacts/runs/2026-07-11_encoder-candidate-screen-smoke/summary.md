# 编码器候选筛选（seed 17）

本轮在 G1 原始异步双流上完成 20 组等预算训练，候选排序只使用公共自监督验证损失。

## 选定配置

| 方法 | 候选 | 归一化选择损失 | 最佳 epoch |
| --- | --- | ---: | ---: |
| chronaris | D | 0.254278 | 1 |
| contiformer | C | 0.000000 | 1 |
| mult | A | 0.210602 | 1 |
| physiology_only | D | 0.209504 | 1 |
| vehicle_only | C | 0.000000 | 1 |

## 证据边界

候选排序使用 96 个训练配置和 23 个验证配置；另保留 1 个 G1 验证配置用于开发确认。本轮未打开任务标签、仿真真值或封存测试数据。

验收检查通过 6/6。

运行参数：max_epochs=1, batch_size=128, patience=1, seed=17。
