# Artifacts

当前产物入口：

- `ARTIFACTS.md`：产物索引和引用规则。
- `runs/`：当前可引用 run roots，按 `YYYY-MM-DD_intent` 命名。
- `archive/`：历史阶段编号报告、旧资产目录和兼容入口。
- `cleanup/`：清理记录。

当前论文协议快照在 `runs/2026-07-03_thesis-protocol-snapshot/`。
