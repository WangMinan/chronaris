你现在负责对私有仓库 `https://github.com/WangMinan/chronaris` 执行一次完整的长程研究与开发任务。你拥有在远程开发机上读取代码、修改文件、运行实验、提交 Git、推送远端、创建与删除分支和标签的权限。

本任务不是只写计划，也不是完成一轮冒烟后停止。你需要依次完成：

1. 清理本地与远端 Git 分支，收束仓库主线；
2. 将需要保留的最新代码和证据整合进 `main`，实际更新远端 GitHub；
3. 审计新下载的 PhysioNet 和 CLARE 数据；
4. 基于现状制定并提交新的研究计划与评价协议；
5. 开发 Chronaris 新主线；
6. 在公开数据和鼎新数据上运行多轮实验；
7. 根据训练内和公开验证结果迭代，最终形成一次锁定的鼎新确认评价；
8. 将代码、紧凑证据、结论和仓库状态同步到 GitHub。

除非遇到无法绕过的权限、凭据、数据损坏或硬件故障，不要中途停下来询问。自行做出合理决策，在文档中记录依据并继续推进。

# 一、研究背景与最高目标

论文题目保持为：

《航空人机异构时序数据的连续对齐与语义融合方法研究》

核心方法名称始终使用 `Chronaris`。不要在论文主线中并列包装多个 Chronaris 版本；旧实现只作为历史研究阶段，新的实现最终仍作为唯一 Chronaris 主模型。

中期考核表中的进度勾选、成果等级和时间安排均不作为本任务约束，不要据此限制开发规模或实验轮次。

中期答辩中唯一需要落实的真实导师意见是：

- 引入明确的下游算法任务；
- 将 Chronaris、MulT、ContiFormer、朴素同步、单流和传统方法输出置于统一输入、统一下游算法和统一评价协议下比较；
- 完善任务定义、评价指标、基线对比、组件消融和结果分析；
- 增强融合方法与机动动作分类、飞行员生理状态评估等实际任务之间的联系。

本任务的最终研究目标不是接受“Chronaris 只在仿真局部有效”作为终点，而是积极寻找并实现一条能够在鼎新真实数据任务上形成优势的 Chronaris 主线。

最低研究晋级目标是：

1. 在至少一个语义合理、预先冻结的鼎新核心双流任务上，Chronaris 超过当前全部融合基线，包括朴素同步、MulT 和 ContiFormer；
2. 在至少一个真正需要跨模态增量的鼎新任务上，Chronaris 超过最佳单流方法；
3. 在机动动作这类航电强势任务上，Chronaris 至少消除当前严重负迁移，稳定接近或超过航电单流；
4. 优势不能只来自单个随机种子、单个窗口或事后挑选子集，应在折级、种子级或预定义事件子集上具有一致性；
5. 同时在至少一个独立公开真实数据集上形成可复现的外部正向证据。

拉伸目标是 Chronaris 在选定的全部鼎新核心任务上超过当前最强基线。

可以激进地调整架构、预训练、任务头、迁移方式、池化方式、损失函数、训练调度和数据组织，但不得：

- 引入未来信息泄漏；
- 使用样本、架次、飞行员、窗口位置等身份捷径；
- 根据锁定确认结果反复修改同一协议；
- 弱化基线、减少基线预算或给予 Chronaris 额外标签信息；
- 只挑有利折、字段、子集或指标；
- 将弱监督任务包装成人工专家真值；
- 将仿真结果冒充真实应用结果。

允许积极研究，但必须保持实验诚信。

# 二、运行环境与数据位置

优先使用仓库当前约定的 Python 环境：

```text
/home/wangminan/env/anaconda3/envs/chronaris/bin/python
````

仓库通常位于：

```text
/home/wangminan/projects/chronaris
```

先自行确认实际路径、远端地址、当前分支、工作树和环境，不要盲目假设。

新数据集路径已经固定：

```text
/home/wangminan/dataset/chronaris/physio_net
/home/wangminan/dataset/chronaris/clare
```

其中：

* PhysioNet 虚拟飞行任务数据作为新的主要公开真实双流外部验证集；
* CLARE 作为跨受试者、多模态生理与认知负荷辅助验证集；
* NASA 和 UAB 不再承担当前主线公开证据角色；
* 旧 NASA/UAB 代码和产物可归档保留以便追溯，但应退出默认任务、当前状态文档和新实验主表；
* 不要删除历史证据，不要继续投入时间维护旧公开路线。

原始数据不得提交到 Git、Git LFS 或公开产物中。PhysioNet 数据按受限数据处理，不在报告中暴露可识别信息。所有公开证据只提交数据清单、匿名统计、配置、哈希、指标和图表。

# 三、阶段 0：先清理分支并实际同步远端 GitHub

这是本任务的第一项硬要求。没有完成分支清理和远端同步前，不开始新模型开发。

先读取：

```text
AGENTS.md
docs/README.md
docs/STATE.md
docs/implementation/TASKS.md
docs/requirements/SPEC.md
docs/artifacts/ARTIFACTS.md
```

然后全面盘点：

* 本地分支；
* 远端分支；
* annotated/lightweight tags；
* worktree；
* 未提交修改；
* 未跟踪文件；
* 每个分支相对 `origin/main` 的 ahead/behind；
* 是否已有 PR；
* 独有提交及其内容；
* 分支是否已被后续工作替代。

先 `fetch --all --tags --prune`。任何未提交修改都不得丢失；必要时先建立安全分支或提交临时保护提交。

基于之前的审查，重点核对并按以下规则处理现有分支。不要盲目执行名称列表，先验证实际存在性、祖先关系和独有提交。

## 3.1 已合入或完全落后于 main 的分支

预期包括：

```text
codex/dingxin-target-reconstruction-20260715
codex/dingxin-task-stability-20260714
codex/dingxin-core-feasibility-20260714
planning/fusion-stream-structure-20260706
```

确认其有效内容已经进入 `main` 或已被后续实现替代后，直接删除本地和远端分支。

## 3.2 有历史价值但不应合入当前主线的分支

预期包括：

```text
codex/chronaris-v2-mainline-20260712
implement/fusion-stream-structure-20260707
```

先创建 annotated archive tag，再删除分支。可使用或调整以下名称：

```text
archive/chronaris-v2-not-promoted-20260713
archive/e3-fusion-structure-mixed-20260708
```

标签必须指向对应分支最后提交，并写明该研究线的结论和关闭原因。

不要把旧分支中的大型产物、旧 checkpoint、重复图件、E3 候选逻辑或带有 `final` 命名的目录整体合入当前主线。只保留确实仍有复用价值的通用代码。

## 3.3 需要整合进 main 后再删除的分支

重点核对：

```text
codex/dingxin-task-aware-safe-residual-20260715
codex/simple-downstream-rebuild-20260715
```

这两条线包含仍需保留的代码、协议和紧凑证据：

* 安全残差研究中的门控残差、无损退化思路及其代码；
* 简化下游评价中的新任务合同、统一消费者、数据隔离、哈希复核和正式负面确认。

先分别创建 annotated evidence tag，例如：

```text
evidence/dingxin-safe-residual-gap-20260715
evidence/simple-downstream-confirmation-20260716
```

然后从最新 `main` 建立一次性整合分支，例如：

```text
chore/consolidate-research-history-20260718
```

将需要保留的两条线合入。冲突重点预计集中在：

```text
docs/STATE.md
docs/implementation/TASKS.md
docs/artifacts/ARTIFACTS.md
```

不要机械选择任一侧。整理后的文档应明确：

* 简化下游评价已经完成，是当前真实数据的最新协议冻结确认；
* 其不利结果保留，不删除、不覆盖；
* 安全残差线冻结阶段无损，但研究晋级门禁未通过；
* 上述两条线均已关闭，不再作为当前执行入口；
* 当前开始新的安全滞后感知融合研究主线；
* 不再根据旧协议已打开的外层结果继续调参。

运行完整或合理范围内的测试，至少确保：

```text
git diff --check
核心单元测试通过
下游评价相关测试通过
表示与模型相关测试通过
```

整合后更新并推送 `main`，再删除这两个旧分支及一次性整合分支的本地和远端引用。

若远端保护规则阻止直接更新 `main`，使用 PR 或 GitHub CLI 完成合并，不要只保留本地结果。

## 3.4 本地未推送历史分支

核对是否存在：

```text
codex/chronaris-core-task-recovery-20260713
```

若存在，确认其中外层支持重叠或协议无效的历史事实，创建：

```text
archive/core-task-recovery-protocol-invalid-20260713
```

随后清理对应 worktree 和本地分支。不要强制删除包含未提交修改的 worktree。

## 3.5 处理未列出的其他分支

对任何未列出的分支使用统一规则：

* 已完整合入 `main`：删除；
* 有独有提交但研究线已关闭：annotated tag 后删除；
* 只有计划或已被实现替代：删除；
* 包含当前仍需复用的代码：选择性整合进 `main` 后删除；
* 真正仍在开发：不得保留多条，最终统一进入唯一新研究分支。

清理完成后的远端活动分支应至多为：

```text
main
research/safe-lag-aware-fusion-20260718
```

若新研究分支尚未创建，则暂时只保留 `main`。

最后重新 `fetch --prune`，输出并保存本地、远端、tag 和 worktree 清单，确认远端 GitHub 已实际更新。不要用报告代替实际推送和删除操作。

除非确有不可合并的历史损坏，不重写 `main` 历史，不使用普通 `--force`。确需重写时，必须先建立远端备份 tag，并只允许 `--force-with-lease`。

# 四、阶段 1：在干净 main 上完成代码、结果和数据审计

分支清理完成后，从最新 `main` 创建唯一活动分支：

```text
research/safe-lag-aware-fusion-20260718
```

先审计，不立刻大规模训练。

## 4.1 审计当前 Chronaris

结合代码和既有实验，重点验证而不是直接假设以下问题：

1. 当前 Chronaris 是否缺少绕过融合投影的单流残差出口；
2. 航电强信号是否在融合和 64 维压缩后严重丢失；
3. 是否对 96 个时间点直接均值池化，导致末端状态、趋势、局部事件和滞后信息被抹平；
4. 同一时刻潜态相似损失是否与生理滞后响应假设冲突；
5. Chronaris 辅助损失是否前 10 epoch 为零，而最佳 checkpoint 经常在第 10 至 15 epoch 出现；
6. 早停是否只依据公共重构损失，导致论文特有机制尚未充分训练；
7. 尺度门控是否塌缩到单一滞后窗口；
8. 表示是否存在低有效秩、维度塌缩、种子不稳定或跨折漂移；
9. 当前物理约束是否作用在正确的航电变量和潜态上；
10. 当前公共预训练目标是否过于偏向原值重构，而没有学习真正的跨流增量。

至少完成：

* 表示方差、有效秩、协方差谱、维度利用率；
* 单流信息线性恢复；
* 不同池化方式的训练内诊断；
* 辅助损失、梯度范数和训练调度审计；
* 门控权重和滞后分布审计；
* 航电单流经过 Chronaris 后的信息损失审计；
* 当前 baseline 实现和输入公平性审计。

将结果写入日期加意图命名的审计目录，不使用 `stage_*` 或 `final`。

## 4.2 审计新数据集

对以下路径进行只读审计：

```text
/home/wangminan/dataset/chronaris/physio_net
/home/wangminan/dataset/chronaris/clare
```

自行处理必要的解压、文件识别和格式读取，但保留原始压缩包和目录，不修改原始文件。

分别确认：

* 文件和目录结构；
* 数据集版本及说明文件；
* 参与者数量；
* 任务或试次数量；
* 生理模态；
* 飞机状态、交互或任务上下文模态；
* 标签及其时间分辨率；
* 时间戳类型和同步方式；
* 各模态采样率；
* 缺失、异常和不可用字段；
* 单位和量纲；
* 受试者、任务、试次和窗口之间的分组关系；
* 可构造的真实下游任务；
* 许可和匿名化边界；
* 数据体积与预计缓存规模。

生成结构化数据清单、匿名统计和哈希。原始文件路径可进入本地配置，但不得进入需要公开传播的报告。

PhysioNet 作为主外部双流数据：

* 生理模态作为人体流；
* 飞机状态、操纵交互和飞行性能作为机器或任务流；
* 优先探索任务难度、着陆性能、飞行事件和事件后生理响应；
* 按参与者和飞行试次分组，禁止窗口随机切分泄漏。

CLARE 作为辅助外部数据：

* 明确它不是航电数据；
* 可将不同生理模态分为中央与外周、脑电与非脑电等有语义依据的双流；
* 主要验证跨受试者认知负荷、异步多模态融合和模态缺失鲁棒性；
* 采用 subject-wise 或 LOSO 评价。

# 五、阶段 2：先制定并提交新的研究计划和评价协议

完成审计后，必须先提交计划和协议，再写新主干代码。

至少新增或更新：

```text
docs/implementation/notes/safe-lag-aware-fusion-research-plan-2026-07-18.md
docs/requirements/safe-lag-aware-fusion-evaluation-v2.md
docs/STATE.md
docs/implementation/TASKS.md
docs/artifacts/ARTIFACTS.md
```

文件名可以根据实际日期调整，但不要出现 `final`。

计划必须明确：

1. 研究问题；
2. 数据角色；
3. Dingxin、PhysioNet、CLARE 的任务定义；
4. 主指标和辅助指标；
5. baseline；
6. 数据划分；
7. 搜索空间；
8. 训练和迁移策略；
9. 晋级门禁；
10. 外层结果打开规则；
11. 消融；
12. 压力测试；
13. 产物合同；
14. 停止条件；
15. 失败后的诊断与下一候选策略。

## 5.1 数据角色

新的证据层级固定为：

* PhysioNet：主要独立公开真实双流验证；
* CLARE：跨受试者多模态生理与认知负荷辅助验证；
* 鼎新：目标域真实航空人机双流与最终竞争性验证；
* 仿真：只承担时间偏移、响应时延、缺失和噪声等机制真值；
* UAB/NASA：历史公开路线，不进入新主表。

## 5.2 下游任务

任务不能只为了追分事后定义，但允许在新协议锁定前根据领域语义和训练数据重新设计。

鼎新至少覆盖：

### A. 机动动作或未来机动状态任务

这是导师明确提出的应用任务之一。

它可能高度由航电单流决定，因此同时承担：

* 机动动作分类或未来机动状态预测；
* 航电信息保真；
* 融合负迁移检查；
* 安全回退能力验证。

目标是 Chronaris 不再像当前结果一样严重破坏航电信息。首先超过 MulT、ContiFormer 和朴素同步，进一步争取超过航电单流。

### B. 飞行员生理状态或事件后生理响应任务

这是主要双流增量任务。

不要只预测高度惯性的未来生理绝对值。优先研究：

* 相对当前状态的未来生理变化；
* 事件条件下的生理响应残差；
* 航电事件后的延迟响应；
* 训练折定义的高响应状态；
* 多字段生理状态评估；
* 飞行状态条件下的生理变化分类或回归。

事件窗口或样本子集必须由输入侧航电事件、训练折规则或预先定义的领域规则确定，不能根据留出样本的未来生理标签筛选。

绝对值预测、持久性基线和全窗口结果仍需保留，以防只报告有利子集。

### C. 时间机制任务

保留一个机制任务，例如：

* 时钟偏移恢复；
* 响应时延恢复；
* 正确事件—响应配对；
* 滞后区间识别。

它用于解释 Chronaris 为什么有效，不替代应用任务。

T3 检索不再作为必须优化的主任务，但可用于困难负样本、跨流对比学习或机制诊断。

PhysioNet 优先选择：

* 飞行任务难度或负荷分类；
* 着陆性能或飞行误差回归；
* 飞机状态与操纵事件条件下的生理响应；
* event-to-response 的滞后或匹配任务。

CLARE 优先选择：

* 认知负荷分类或回归；
* LOSO 跨受试者评价；
* 模态缺失与双流增量；
* 不同生理流组合的负迁移分析。

最终任务以实际数据审计为准。

# 六、阶段 3：开发新的 Chronaris 单主线

不要从零推倒成熟的数据冻结、审计、checkpoint、resume、统一表示和报告基础设施。保留可靠工程能力，重构模型和训练协议。

新的 Chronaris 至少应系统解决以下问题。

## 6.1 安全融合与单流保真

核心结构应保留模态私有表示，并把跨模态融合限制为增量，而不是强制覆盖强单流信息。

优先考虑：

```text
z_out = [z_phys_private, z_vehicle_private, gate * z_cross]
```

或者任务输出级安全残差：

```text
logits = logits_best_single + gate * delta_cross
prediction = prediction_baseline + gate * residual_cross
```

门控应：

* 在初始化时接近安全回退；
* 只根据当前输入和训练数据学习；
* 支持模态缺失；
* 防止融合分支破坏单流分支；
* 输出可审计的权重。

可以使用 mixture-of-experts、残差门控、冻结单流教师、知识蒸馏或分阶段解冻，只要协议中明确标签使用和公平性。

## 6.2 私有与共享表示解耦

生理和航电具有不同动力学，不要强迫全部潜态同刻相似。

至少区分：

* physiology-private；
* vehicle-private；
* lag-aware shared/cross-modal；
* modality availability；
* uncertainty/reliability。

应能分别从最终表示恢复关键生理和航电历史统计，以证明融合没有抹除单流信息。

## 6.3 滞后感知条件对齐

将“同一时刻余弦相似”改为更符合论文假设的目标，例如：

* 历史航电预测随后生理变化；
* 正确滞后优于错误滞后；
* event-conditioned contrastive learning；
* 相似飞机状态下区分正确与错误响应时间；
* 多尺度连续时间延迟核；
* 可解释的响应时延分布。

负样本要避免只靠飞行阶段或绝对位置区分。

## 6.4 更合理的时序汇聚

不要只使用整个 30 秒序列的简单均值。

比较并冻结一种方法无关或公平的汇聚合同，例如：

* last state；
* mean/std；
* slope/trend；
* recent-weighted pooling；
* 多尺度窗口统计；
* 少量因果 query token；
* attention pooling；
* event-centered pooling。

主结果可以同时保留统一冻结表示和任务感知序列消费者两条轨道：

1. 冻结表示 + 相同线性或浅层消费者，评估表示质量；
2. 相同预算的端到端或任务感知消费者，评估真实下游性能。

不能再用单一均值池化加 Ridge 代表整个模型能力。

## 6.5 分阶段训练

训练流程至少考虑：

1. 模态私有编码器和基础重构；
2. 跨模态滞后机制训练；
3. 安全残差和门控训练；
4. 下游任务感知微调；
5. 必要时部分解冻。

论文特有辅助损失不能长期为零，也不能在其刚启用时就因公共重构损失早停。

checkpoint 选择应结合：

* 公共无标签损失；
* 独立时间机制验证；
* 单流信息保真；
* 训练内下游验证；
* 负迁移门禁。

不要直接使用鼎新锁定外层指标选 checkpoint。

## 6.6 公开预训练与目标域适配

充分利用新数据集。

PhysioNet 可用于：

* 生理—飞行状态双流自监督预训练；
* event-to-response 滞后学习；
* 飞行性能和负荷任务监督预训练；
* 缺失与采样抖动增强；
* 公开任务上的模型选择。

CLARE 可用于：

* 生理编码器预训练；
* 跨受试者稳定性；
* 多生理模态融合；
* 模态缺失；
* 负荷表示学习。

由于字段 schema 不完全一致，可以采用：

* 重新初始化输入投影、迁移时间主干；
* 字段组或语义类别适配；
* feature-token 编码；
* 共享时间动力学和数据集特异输入层；
* 无标签目标域适配；
* CORAL、MMD、对抗域适配等训练内候选。

任何迁移都必须记录复制参数范围、冻结策略和数据角色。

## 6.7 物理约束

物理约束只作用在有明确单位和动力学语义的航电变量、航电解码器或航电私有潜态上。

不要把生理—航电共享潜态强行承担全部物理残差。

验证：

* 物理分量实际激活；
* 梯度规模合理；
* 不因单位或尺度产生异常；
* 不损害主要任务；
* 消融结果按任务解释，不要求所有任务一致正增益。

# 七、阶段 4：实验策略与积极迭代

不要在第一轮不理想后立即停止或转写论文。至少执行多个有明确假设的候选波次。

建议按以下顺序推进，但可以根据审计结果调整。

## 7.1 波次 A：修复信息损失

重点测试：

* 单流旁路；
* 安全残差；
* 多统计量池化；
* 辅助损失调度；
* 单流信息恢复；
* 无跨模态分支控制组。

目标是先让 Chronaris 在机动任务上接近航电单流，并显著超过当前 Chronaris。

## 7.2 波次 B：学习真正的跨流增量

重点测试：

* lag-aware conditional prediction；
* event-conditioned contrastive loss；
* 正确/错误响应时间辨别；
* 私有/共享解耦；
* 困难负样本；
* 输入侧事件采样。

目标是在生理状态或事件后响应任务上超过最佳单流和融合基线。

## 7.3 波次 C：公开预训练与迁移

重点测试：

* PhysioNet 双流预训练；
* CLARE 生理编码器预训练；
* 多数据集自监督；
* 训练折内无标签适配；
* 部分冻结和逐层解冻；
* 单流教师蒸馏；
* 域适配。

目标是在公开外部数据和鼎新训练内验证中同时改善，而不是只适配鼎新两个架次。

## 7.4 波次 D：任务感知优化

在统一表示比较之外，允许使用公平的任务感知训练：

* 轻量 MLP；
* TCN；
* 因果 Transformer head；
* Ridge/Logistic；
* MiniRocket；
* 梯度提升；
* mixture-of-experts；
* residual correction head。

所有方法使用相同任务输入、相同分组和合理的预算。可以给每个方法使用其适配的标准训练方式，但必须同时保留一条统一消费者比较轨道。

使用多保真候选筛选、断点恢复和结构化实验注册。优先在公开数据和鼎新 inner-validation 上搜索，不读取新的锁定外层结果。

每一轮失败后先做误差和表示诊断，再决定下一轮修改。不要无目的扩大超参数网格。

# 八、数据划分、基线和指标

## 8.1 划分

所有数据都禁止窗口级随机泄漏。

鼎新：

* 留一架次仍是主要跨架次确认单位；
* inner-development 使用训练架次内部的时间块、视图或严格隔离单元；
* 输入 30 秒和目标 5 秒的完整支持区间不得跨角色重叠；
* 时间隔离缓冲至少覆盖完整上下文加目标；
* 同一 vehicle-context 的多视图不得重复计权；
* 因为历史上外层结果已被查看，最终只能写成“协议冻结后的分组确认”，不能声称盲测。

PhysioNet：

* 以参与者或参与者—试次为分组；
* 主评价优先采用 LOSO 或 group-held-out；
* 同一参与者的窗口不能跨训练和测试。

CLARE：

* 采用 subject-wise/LOSO；
* 标签时间窗重叠必须审计；
* 不允许同一受试者相邻窗口跨角色泄漏。

## 8.2 基线

至少包括：

* physiology-only；
* vehicle/context-only；
* persistence/current-state；
* naive time synchronization；
* early concatenation；
* MulT；
* ContiFormer；
* 强传统或浅层基线；
* 必要时 MiniRocket、梯度提升或 TCN。

先审计 baseline 的输入、参数和训练是否合理，再冻结基线配置。不能通过削弱 baseline 获得优势。

论文中写明 MulT 和 ContiFormer 是当前仓库针对本数据合同的适配实现，除非确实完成官方实现的忠实复现。

## 8.3 指标

分类至少报告：

* macro-F1；
* balanced accuracy；
* AUPRC 或 normalized AP；
* 每类召回；
* 混淆矩阵；
* worst-fold；
* 相对最佳单流增量。

回归至少报告：

* RMSE；
* MAE；
* Spearman；
* 相对持久性或当前状态的 skill；
* 字段级结果；
* 正技能字段比例；
* worst-fold。

融合专用指标至少包括：

* 相对最佳单流增量；
* 负迁移率；
* 模态缺失时的退化；
* 门控回退率；
* 表示有效秩；
* 单流信息恢复；
* 滞后恢复误差。

仅有两个鼎新架次时不做夸大的显著性结论。以折级一致性、种子一致性、效果量和机制证据为主。

# 九、晋级门禁与锁定确认

在协议中预先冻结具体阈值。阈值应根据训练内和公开验证的基线尺度确定，不得根据新的鼎新外层结果事后修改。

最低晋级门禁包括：

1. 数据和时间边界审计全部通过；
2. Chronaris 表示不再明显丢失航电单流信息；
3. 公开数据至少一个主任务上超过最佳相关 baseline，并在多数折或受试者上形成正增量；
4. 鼎新 inner-development 上至少一个双流核心任务超过全部融合 baseline；
5. 鼎新 inner-development 上至少一个任务超过最佳单流；
6. 机动任务的安全退化满足预先冻结容差，争取直接超过航电单流；
7. 关键消融能够解释安全残差、滞后机制或私有/共享解耦的贡献；
8. 多随机种子结果稳定；
9. 缺失和抖动压力下没有灾难性退化。

只有上述门禁满足后，才运行一次新的鼎新锁定分组确认。

锁定确认前必须：

* 固化 commit SHA；
* 固化数据清单和哈希；
* 固化任务公式；
* 固化字段；
* 固化 split；
* 固化模型；
* 固化 checkpoint 选择规则；
* 固化消费者；
* 固化指标；
* 固化图表模板；
* 保证确认运行不可读取外层指标进行中途决策。

确认结果打开后，不在同一协议版本上继续调参。若结果仍不理想，只能如实记录，并另起明确的新研究问题和协议版本；不得静默覆盖旧结果。

# 十、消融与解释

至少完成以下紧凑消融：

* 无安全残差；
* 无单流旁路；
* 无滞后目标；
* 同刻对齐替代滞后对齐；
* 无私有/共享解耦；
* 简单均值池化；
* 无公开预训练；
* 无物理约束；
* 无模态缺失训练。

消融不要求每个组件在每个任务上都正向，但必须能回答：

* 哪个组件负责单流保真；
* 哪个组件负责跨流增量；
* 哪个组件负责滞后恢复；
* 哪个组件负责缺失鲁棒性；
* 物理约束在哪些任务有效、在哪些任务有代价。

保存代表性门控权重、滞后分布、事件响应案例和错误案例，但不得挑选只有 Chronaris 获胜的样本。案例选择规则必须预先定义。

# 十一、工程与仓库要求

继续复用并改进：

* 原始数据冻结与哈希；
* manifest；
* checkpoint registry；
* resume；
* 训练/验证/确认角色隔离；
* CPU/GPU 稳定性；
* 测试；
* 结构化 unavailable；
* 紧凑证据包。

要求：

* 使用单一权威入口；
* 不再新增大量平行脚本；
* `scripts/` 只做命令编排；
* 核心逻辑进入 `src/chronaris/`；
* 测试进入合理拆分的 `tests/`；
* 文档和产物使用“日期 + 意图”命名；
* 不使用 `stage_h`、`stage_i` 或 `final`；
* 重型 checkpoint、原始预测、稠密表示和缓存放入被忽略目录；
* Git 只提交代码、配置、哈希、紧凑指标、图表和结论；
* 每个长运行都可恢复；
* 每个阶段完成后提交一次有意义的 commit 并推送；
* 定期检查 GPU 利用率、数据加载、AMP、pin memory、batch、缓存和并发；
* 不为追求 GPU 满载牺牲确定性和数据隔离。

# 十二、阶段 5：证据收口和远端状态

完成有效实验后：

1. 更新 `docs/STATE.md`，只保留一个当前状态；
2. 更新 `docs/implementation/TASKS.md`，关闭完成任务；
3. 更新 `docs/artifacts/ARTIFACTS.md`；
4. 生成新的实验报告、结果表、消融表和图；
5. 明确区分公开数据、鼎新、仿真和弱监督证据；
6. 保留所有不利结果和边界；
7. 运行完整测试和 `git diff --check`；
8. 推送唯一研究分支；
9. 若晋级门禁和确认评价完成，则将其合入 `main` 并删除研究分支；
10. 若研究仍需继续，则远端仅保留 `main` 和这一条研究分支，不再新建并行活动分支。

最终远端 GitHub 不应再次出现大量完成后未删除的 `codex/*`、`implement/*` 或 `planning/*` 分支。

# 十三、最终交付报告

任务结束时，在仓库中生成一份中文长程执行报告，至少说明：

* 分支清理前后状态；
* 合入、打 tag、删除的具体分支；
* 远端 GitHub 最终分支和 tag；
* PhysioNet 和 CLARE 数据审计结论；
* 新任务定义及其语义依据；
* 新 Chronaris 架构；
* 相对旧模型修复了什么；
* baseline 公平性；
* 公开数据结果；
* 鼎新训练内结果；
* 锁定确认结果；
* 消融；
* 压力测试；
* 是否达到最低晋级目标和拉伸目标；
* 当前最强配置和 commit SHA；
* 所有关键产物路径；
* 尚存问题；
* 下一步唯一建议。

不要只报告“流程完成”或“门禁未通过”。报告必须解释性能变化来自哪里，以及下一步最值得继续的研究杠杆是什么。

执行时以实际仓库、实际数据和实际结果为准。可以修正上述建议中的具体技术方案，但必须保持最高目标：在不牺牲实验诚信的前提下，积极推进 Chronaris 在鼎新真实下游任务上超过当前 baseline，并建立公开真实数据上的独立证据。
